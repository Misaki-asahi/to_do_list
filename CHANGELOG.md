# 更新日志

本项目的所有重要变更都会记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **版本号怎么读**
> `主版本.次版本.修订号`，例如 `0.1.0`。
> - **主版本**：有不兼容的大改动时 +1（比如数据库结构变了、老数据要迁移）
> - **次版本**：加了新功能时 +1
> - **修订号**：只修 Bug、不改功能时 +1
>
> `0.x.y` 表示「还在早期，接口可能变」。当前就是这一阶段。

---

## [未发布]

### 计划新增
- 前端网络断开时的友好错误提示（OPT-013）。
- 「撤销」按钮：删除后几秒内可一键撤回，不用专门去回收站（OPT-001 的体验延伸）。

### 计划变更
- 把前端 DOM 交叉检查固化为通用脚本 `scripts/check_frontend.py`（OPT-017，目前在回收站页已有单页版本）。
- 继续拆分过长函数：`scripts/md2docx.py`、`scripts/clean.py`。

---

## [0.2.0] - 2026-09-13

**本次的重点是「数据安全」** —— 堵上项目里风险最高的一处缺口：误删无法找回。

### 新增

**回收站（软删除）**
- 删除不再真正销毁数据，而是移入回收站，**随时可以还原**
- 新增「回收站」页面：查看已删除任务、还原、彻底删除、清空回收站
- 彻底删除与清空回收站都有**二次确认**，且只能作用于已在回收站里的任务
- 已删除的任务不会出现在列表、搜索、统计、日历里，也**不会再弹提醒**
- 列表页的删除提示改为「会先被放进回收站」，不再吓唬用户

**数据库自动升级**
- 老版本创建的数据库会在启动时**自动补上新字段**，不需要用户做任何事
- 升级只做「加列」，不触碰任何已有数据；同时内置幂等保护，重复启动不会出错

### 修复

- **BUG-019**：脚本输出被重定向到文件时崩溃。
  输出里的 `✅` 在 GBK 编码下无法表示，直接抛 `UnicodeEncodeError`；
  更糟的是它崩在最后一行，导致「前面全跑完了，结果一个字都没写进文件」。
  已改为纯文本标记，并新增 `scripts/_console.py` 作为通用输出保护。
- **BUG-020**：某个脚本里硬编码了开发者本机的绝对路径（形如 `X:\...\to_do_list`）。
  会导致项目换台电脑就失效，还把本机目录结构泄露到了公开仓库。已改为从文件位置推算。
- **BUG-021**：自检脚本跑第二遍必然误报失败。
  日志是跨次运行累积的，而断言在数日志里的行数，于是越跑错得越多。
  已在导入应用前清空隔离日志，现在连跑三次都是「全部通过」。
- **BUG-018**：时间解析过于宽松，`2026-9-13 8:30` 这种没补零的格式也会被接受，
  导致非规范字符串进库、破坏「字符串排序即时间排序」的前提。已加入往返校验。

### 变更

- 版本号 0.1.0 → 0.2.0
- 自检工具与测试脚本统一使用输出保护，避免编码问题再次打断检查
- 回收站页面使用 DOM API 而非 `innerHTML` 渲染，从原理上杜绝 XSS

### 测试

- 测试总数 229 → **259 项，全部通过**
- 新增 `tests/test_trash.py`，覆盖数据库迁移、软删除语义、四个新接口、路由顺序回归等

### 安全

- 真实数据库的升级流程已在**副本**上做过完整演练：14 列 → 15 列，数据条数与内容均无变化
- 本版本关闭了「误删即永久丢失」这一风险点

### 打包

- 已重新打包并完成端到端验证：免安装版可直接运行、安装版可安装/运行/卸载
- 安装包体积 32.6 MB，免安装版 9.7 MB

---

---

## [0.1.0] - 2026-09-13

第一个可用版本。**离线待办清单** —— 本地优先，断网可用，数据只存在自己电脑上。

### 新增

**待办管理**
- 添加 / 查看 / 编辑 / 删除待办
- 标记完成与取消完成，完成时间自动记录
- 按状态筛选：全部 / 未完成 / 已完成
- 关键字搜索（同时匹配标题、描述、分类）
- 优先级（低 / 中 / 高）、分类、详细描述、截止时间
- 完成情况统计

**提醒系统**
- 提醒时间支持**分钟级**精度（`YYYY-MM-DD HH:MM`）
- 后台守护线程每 20 秒扫描一次到点任务
- **防重复提醒**：靠数据库里的 `reminded_at` 字段保证只弹一次，
  程序重启不会重弹，关机期间到点的提醒开机后也能补上
- **Windows 原生通知**：右下角弹出、不点不走，浏览器关掉也能收到
- 网页内右下角常驻提醒卡片，支持「完成」和「稍后提醒」
- 可选提示音开关

**日历**
- 月历视图，有任务的日期会标出数量
- 点击未来任意一天即可新建当天的任务
- 查看某一天的任务明细

**设置**
- 开机自启开关（可同时选择是否自动打开浏览器）
- 手动备份数据库、一键打开数据文件夹
- 在线查看运行日志、手动注册通知身份
- 测试通知按钮（一键验证提醒能不能弹）

**工程化**
- 三层架构：`routers` → `services` → `repositories`
- 统一异常体系与全局异常处理
- 文件日志（按天轮转，保留 14 天）+ 未捕获异常自动记录
- 启动时自动备份数据库，保留最近 7 份
- 229 项自动化测试（pytest），测试全程在隔离数据库中进行
- 自检脚本：数据层自检 34 项、异常处理自检 30+ 项、冒烟测试
- 一键打包：`build.bat` 生成免安装版，`build_installer.bat` 生成安装程序
- 自带安装器（不依赖 Inno Setup / NSIS），支持静默安装与卸载
- 双击即用的 `.bat` 入口：启动 / 自检 / 清理 / 打包 / 提醒测试

### 变更

- **编辑界面改为弹窗**：原先就地填回顶部表单，不够突出、容易看漏（用户反馈）。
- **提醒升级为系统级通知**：原先只在网页内提示，关掉浏览器就收不到；
  现在由后端线程直接调用 Windows 系统通知（用户反馈）。
- 时间解析增加**往返校验**：只接受严格补零的格式，防止非规范字符串污染数据库、
  破坏「字符串排序即时间排序」这一前提（见 BUG-018）。
- 通知身份默认使用「自动」模式：优先用系统已注册的身份，避免通知被 Windows 静默丢弃。
- 依赖精简：提醒调度改用标准库 `threading`，通知改用系统自带 PowerShell，
  **运行时依赖只有 4 个**（fastapi、uvicorn、sqlalchemy、pydantic）。

### 修复

**启动与环境**
- 启动脚本双击后窗口一闪就退（BUG-001）
- 输出被重定向时启动横幅不可见（BUG-010）
- 模块文档字符串里的非法转义序列（BUG-017）

**前端**
- 页面残留对已删除 DOM 元素的引用（BUG-008、BUG-015）

**后端**
- `/api/tasks/stats` 被 `/api/tasks/{task_id}` 抢先匹配导致参数校验失败（BUG-006）
- 异常类继承顺序错误导致启动即报错（BUG-007）
- 日历接口对非法年月参数直接返回 422，容错逻辑来不及生效（BUG-009）
- 时间解析过于宽松，接受了没补零的非规范格式（BUG-018）

**提醒**
- 后台线程因引用已改名的常量而静默死亡，提醒整个失效（BUG-012）
- Windows 因通知身份未注册而**静默丢弃**所有通知（BUG-013）
- 提示音被错误地当作「通知已送达」而被关闭（BUG-014）

**文档与工具**
- 文档里的转义字符残留、日期写错、用户找不到脚本目录（BUG-002、BUG-003、BUG-004）
- 测试数据库被写进系统临时目录，违反项目自包含原则（BUG-005）
- 提醒测试脚本的菜单死循环，批量创建了 108 条多余任务（BUG-016）

### 安全

- **修复一次数据丢失事故（BUG-011）**：一个临时测试脚本误操作了真实数据库，
  导致用户手动创建的待办全部丢失且无法恢复。由此确立并落实了三项机制：
  1. **测试隔离**：任何写数据的脚本必须先切换到 `data/test/` 下的隔离数据库，
     并在导入应用后二次校验（`scripts/_isolation.py`）。
  2. **危险操作需显式确认**：会清空数据的函数必须传 `confirm=True`，否则抛异常。
  3. **自动备份**：程序每次启动自动备份数据库，保留最近 7 份。
- `.gitignore` 排除整个 `data/` 目录，确保**私人待办数据永远不会被上传到 GitHub**。

---

## 版本说明

- 本文件记录的是**面向使用者**的变更。
- **详细的 Bug 分析**（症状 / 复现步骤 / 报错原文 / 根因 / 修复 / 验证 / 经验教训）
  见 `docs/问题记录与解决方案.md`。
- **待优化清单**见 `docs/待优化与已知问题.md`。
- **项目当前进度与架构说明**见 `PROJECT.md`。
