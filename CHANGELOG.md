# 更新日志

本项目的所有重要变更都会记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **版本号怎么读**
> `主版本.次版本.修订号`，例如 `0.1.0`。
> - **主版本**：有不兼容的大改动时 +1（比如数据库结构变了、老数据要迁移）
> - **次版本**：加了新功能时 +1
> - **修订号**：只修 Bug、不改功能时 +1
>
> `0.x.y` 表示「还在早期，接口可能变」。当前就是这一阶段。

---

## [0.4.5] - 2026-09-15

### 这一版做了什么

发布前的**第二轮 bug / 安全性复查**（用户要求"再查一遍，然后发布"）。
查出 **1 个安全缺口**（BUG-055）并修掉，其余逐条复核通过。

### 修复

**BUG-055：跨站防护只比主机名、不比端口。**

v0.4.0 为修 BUG-043 加的跨站防护，判断"是不是本机来源"时**只看了主机名**
（`127.0.0.1` / `localhost` / `::1`），**没看端口** ——
于是你在本机跑着的**任何网页应用**（比如另一个开发服务器 `127.0.0.1:5173`）
上的页面都被当成"自己人"，能发简单请求**改你的数据、关掉悬浮窗、退出整个程序**。

现在连端口一起比：只有"我们自己这个服务"的页面才算自己人。
命令行（curl）和悬浮窗进程不发 `Origin`，不受影响。

### 第二轮安全复查结论（逐项）

| 查什么 | 结论 |
| --- | --- |
| 跨站：恶意 Origin / `Origin: null` / `Sec-Fetch-Site: cross-site` | ✅ 9 个接口**全部 403**（含"退出程序""关窗口"） |
| 跨站：**本机别的端口**（5173 / 9999 / 80） | ✅ **本次收紧后全部 403**（修前是 200） |
| 跨站：自己页面、不带 Origin | ✅ 照常放行（不影响正常使用和调试） |
| SQL 注入（`%` / `_` / `' OR 1=1 --` / `'; DROP TABLE` / `\` 等 6 种） | ✅ 命中 **0** 条、表完好（BUG-041 的修复仍然有效） |
| XSS | ✅ 列表页/日历页对标题、描述、分类、时间**全部 escapeHtml**；回收站页用 DOM `textContent`（更安全） |
| 路径穿越（`/static/../data/todo.db` 等 4 种写法） | ✅ 全部 404 |
| 畸形输入（超长标题/描述、错类型、非法优先级/日期、空对象） | ✅ 一律 **4xx**，没有一个 500 |
| 悬浮窗令牌 | ⚠️ **写清楚**：`GET /api/floating` 不需要凭据就会返回令牌。它**不是鉴权边界**（整个任务接口本来就不需要令牌，本机程序直接就能读写），所以这一版**不动它**，只如实记录 |

### 测试

- **新增 `tests/test_cross_site.py`（7 条）** —— 这道防线从 v0.4.0 加进来之后
  **一条测试都没有**，所以"只比主机名"的缺口一直没人发现。现在把 8 种来源钉死。
- 全量测试、`check_repo.py`、`check_errors.py` 全部通过。

### 打包

重新打包并在**安装版（exe）**里实测：程序能起、**悬浮窗能开出来**、数据目录正确、
退出干净（详见 v0.4.4 那一节的方法）。

---

## [0.4.4] - 2026-09-15

### 这一版做了什么

**一轮"真机实测"验收**：把上一轮交接说明里列的 16 条薄弱点逐条独立复核
（真的把窗口画出来点、真的起子进程、真的用 Chrome 点网页端、像素级量尺寸），
查出 **6 个真实缺陷**（BUG-049 ~ BUG-054），**全部修掉**，并给每条补了防复发的测试。

一句话概括这轮：**"看代码觉得没问题"和"真的点一遍"之间，还隔着一堆缺陷。**

### 修复（按严重度）

| 编号 | 一句话 | 影响 |
| --- | --- | --- |
| BUG-049 | 日期写「上午9点」被算成 **21:00**、「凌晨2点」被算成 **14:00** —— 时段词差 12 小时 | **提醒会在错误的时刻响** |
| BUG-050 | 关 / 重启悬浮窗每次卡 **3 秒多**，最后窗口是被强杀的；重启后设置页还显示"窗口是隐藏的" | 体验 + 状态说假话 |
| BUG-051 | 每次强杀窗口都在日志里留一条 **假 CRITICAL**（`UnicodeDecodeError`） | 日志被假崩溃淹没 |
| BUG-052 | 标题较长时右边被**硬裁**（折行宽度少算了右边那列） | 看不清标题 |
| BUG-053 | 提醒发出后日志里**没有任何痕迹** | 用户报"没收到提醒"时查不下去 |
| BUG-054 | BUG-040 声称已修复，代码里却没有；**照它说的改会让开机自启失效** | 文档与实现不符 |

> 上一轮交接说明里被点名的三处"从来没被真人点过"的地方，这一轮的结论是：
> **✎ 编辑按钮正常**（4 种情形全过）、**设置弹窗取色器正常**（真的弹出面板、选的颜色真的写进库）、
> **设置页 6 个配色色块正常**（真浏览器逐个点，颜色都变了，而且**不动不透明度**）。

### 改进

- **提醒日志**：发出成功记一条 INFO，失败记一条 WARNING（并说明"下次会重试"）。
- **折行宽度按行算**：`_reflow()` 不再用"窗口宽 − 80"猜，而是减掉这一行右边真实占用的宽度。
- **§errors="replace"§**：三处抓 Windows 命令输出的地方（tasklist / taskkill / powershell）。
- **启动横幅说实话**：那句「数据目录」以前印的是 §BASE_DIR§（用 §TODO_DATA_DIR§ 覆盖过时两者不一样），
  本轮排查打包版时被它误导过一次；现在改成印真正的 §DATA_DIR§，并额外印一行「程序目录」。
- **「窗口正在启动中」**：连点两次「重启窗口」时那句「刚刚已经启动过了…」，
  在用户明确点了重启的语境下读着像出错；改成「窗口正在启动中（x 秒前已拉起），请稍等一两秒」（OPT-034）。

### 保持不动的

- **开机自启的行为**（`start.bat` / `start_silent.bat` 不加 `%*`）——
  实测证明"加 `%*`"会让登录时只起窗口、不起服务，详见 BUG-054 与 OPT-032。
- 提交者身份、以及启动文件夹里那条 `.vbs`（按用户要求，等发布之后再清理）。

### 测试

- 全量测试通过；`check_repo.py`（35 项）、`check_errors.py` 全部通过。
- 新增测试：`TestFlexiblePeriods`（4 条，时段词方向）、`TestSubprocessDecoding`（2 条，解码参数）、
  `test_wrap_length_leaves_room_for_the_right_column`（1 条，折行让位）。
- 改 2 条老测试的期望值（它们断言的是"窗口宽 − 80"那个旧公式）。

### 系统提醒这条链路 —— 这回是"肉眼确认"过的

上一轮只调过「发一条测试通知」的接口就写进了结论。这一轮：

1. 让一条真实待办**自然到点**：`reminded_at` 只写了一次、
   `data/notify/toast.xml` 在**那一秒**被写成了那条待办的内容（说明通知真的发出去了）；
2. 再**当场发一条和真实提醒一模一样**的通知（带提醒音），请用户看着屏幕确认 ——
   用户答复：**「看到了，而且带声音、不点不走」**。

顺带澄清一条旧结论：BUG-013（2026-09-13）当时认为"自己往注册表写的身份"**不显示**，
所以默认借用系统身份。这一轮实测：**两种身份现在都能显示**，自动模式选中"自有身份"没有问题。

### 数据安全

本轮全程使用隔离环境（`TODO_DATA_DIR` 指向 `data/test/` 下的目录），
**真实数据库 `data/todo.db` 未被动过**（跑前跑后大小与时间戳一致）；
用户 Windows 启动文件夹里那条 `.vbs` 也**没有被写过**（跑前跑后修改时间一致）。

---

## [0.4.3] - 2026-09-15

### 这一版做了什么

修用户报的一个**会让人白打一遍字**的问题，并按要求把日期输入放宽。

**用户原话：**

> 悬浮窗新建待办，设置提醒日期时日期格式错误会导致整个待办消失，
> 修复这个bug并且放宽日期格式，如：中英文皆可，
> 以用户当前系统时间为准允许输入"某某分钟后"提醒

### 修复

**BUG-048：提醒日期写错 -> 整条待办一条都建不出来。**

实测确认：提醒时间填 `30分钟后` / `2026/09/16 15:00` / `下午3点`，
**待办数都是 0** —— 标题、分类、描述全白填。

两个根因：

1. 日期只认死的 `YYYY-MM-DD HH:MM`，别的写法一律拒绝；
2. **新建被拆成了两次请求**：先建任务、再补截止时间和描述。
   第一次失败 = 全丢；第二次失败 = 任务建了但缺字段，而弹窗已经关了，
   用户以为没存上就重打一遍 —— 列表里多出一条重复的。

现在：**一次请求原子完成（要么全成、要么全不成）**，
失败时**弹窗不关、输入不丢**，错误提示是中文的。

### 新增：宽松的日期输入

新增 `timeutil.parse_flexible()` —— 时间解析的**唯一出口**：

| 可以这样写 | 会存成 |
| --- | --- |
| `30分钟后` / `半小时后` / `in 30 minutes` | 现在 + 30 分钟 |
| `2小时后` / `1天后` / `一周后` | 相应的时长之后 |
| `明天` / `后天` / `tomorrow` / `下周三` / `next monday` | 那一天（沿用现在的时刻） |
| `9月16日` / `2026/09/16 15:00` / `2026.9.16` / `Sep 16 15:00` | 那一天 00:00 |
| `下午3点` / `晚上八点半` / `3pm` / `中午12点` | 当天该时刻（已过则顺延明天） |

规则说明（**没写时刻时怎么定**）：

- 说"哪一天"（明天 / 下周三 / tomorrow）-> **沿用现在的时刻**
- 说"某个日期"（9-16 / Sep 16）-> **00:00**
- 相对时间（30分钟后 / 2小时后）-> 精确算出来

**相对时间以你电脑当前时间（系统时间）为基准**，由服务端和悬浮窗共同使用同一套解析。

### 改进

- **接口现在也接受宽松写法**：翻译成规范格式后再入库，
  所以数据库里永远是定宽格式，**"字符串排序 == 时间排序"这条保证没有丢**。
- **报错说人话**：不再把 Pydantic 的英文原文甩到界面上，
  改成「看不懂这个时间：xxx。可以写『30分钟后』『明天』『9-16 15:00』…」。

### 测试

- 新增 `TestParseFlexible`（19 项）：把各种写法都钉住，
  且**注入固定的"现在"**，保证测试和运行时刻无关（BUG-032 / BUG-045 的纪律）。
- 调整 2 条旧测试：它们原本断言"不补零的格式必须被拒绝"（BUG-018 的老结论）。
  新行为是"**接受，但补零后再入库**" —— 要保住的从来是"库里是定宽格式"，
  而不是"用户必须自己补零"。
- 全量测试、`check_repo.py`、`check_errors.py` 全部通过。

---

## [0.4.2] - 2026-09-15

### 这一版做了什么

**按用户要求调整"出厂设置"** —— 让新装的机器（以及点"恢复默认设置"之后）
直接就是用户自己现在正在用的那套样子，并且开机就能用。

### 改动一览

| 项 | 改前 | 改后 | 用户原话 |
| --- | --- | --- | --- |
| 默认字号 | 9 磅 | **10 磅** | 「字体字号…默认设置为我的当前设置内容」 |
| 默认不透明度 | 0.70（透 30%） | **0.90（透 10%）** | 同上 |
| 悬浮窗总开关 / 显示 | 默认**关** | 默认**开** | 「悬浮窗默认启动」 |
| 开机自启 | 默认**关** | 默认**开，且静默**（不弹浏览器） | 「app默认开机静默自启」「自启动静默启动，不要开启浏览器」 |
| 顶部标题栏留白 | 上下 3 px（栏高 26） | 上下 **6 px**（栏高 **34**） | 「上边标题部分…留白需要放的更宽一下」 |
| 底部输入条留白 | 上下 3/2 px（条高 48） | **9/8 px**（条高 **68**） | 「下输入条留白需要放的更宽一下」 |

### 关于"开机自启默认开启"，具体是怎么做的

**不是"每次启动都强制打开"** —— 那样「关闭开机自启」按钮就成摆设了。
实际逻辑是（`setting_service.ensure_autostart_by_default()`）：

    ① 用户主动关过（新设置键 autostart_user_disabled == "1"）-> 什么都不做
    ② 启动文件夹里已经有我们的 .vbs                        -> 什么都不做
    ③ 以上都不是                                          -> 建一个静默自启项

判断顺序不能换：第 ① 条代表"用户的明确意愿"，必须排在最前面。

建出来的 `.vbs` 内容（**静默**）：

    shell.Run "cmd /c """ & projectDir & "\start_silent.bat"" --no-browser --floating", 0, False

即：开机时程序在后台安静启动（提醒照常响），**不弹出浏览器窗口**，
但**把桌面悬浮窗显示出来**。

### 关于「启动时同时打开浏览器」这个开关

**保留**（用户明确说"不需要拿掉"），只是**出厂默认是关的**。
想要开机就看到网页界面的人，可以自己去设置页把它打开。

### 兼容性说明

- **老用户库里已经存过的设置，一律以库里的为准** —— 默认值只在"没有记录"时生效。
  所以这次改默认值，**不会**把任何人已经调好的外观改掉。
- 开机自启是例外：它属于"新增的默认行为"，
  所以对已有安装也会补建一次（除非用户主动关过）。

### 修复

- **BUG-047：跑一次测试，就往真实的 Windows 启动文件夹里写了一个自启项。**
  "默认开启开机自启"这句逻辑挂在服务启动钩子里，而测试用的 TestClient
  **也会触发启动钩子** —— 于是跑一次 `pytest`，开发者真实的
  启动文件夹里就多出一个 `.vbs`。

  **根因：「数据隔离」不等于「系统隔离」。**
  数据库跟着 `TODO_DATA_DIR` 走（隔离得住），
  但启动文件夹固定在系统路径上，**它不认这个环境变量**。

  修法是两道网：
  1. 代码侧：数据目录不是"默认那个"（= 测试 / 隔离运行）就直接跳过，不碰系统启动项；
  2. 测试侧：`tests/conftest.py` 新增装置，把启动目录换成项目内的临时目录。

  验证：跑测试前后，真实 `.vbs` 的修改时间**完全没变**。

### 测试

全量测试、`check_repo.py`、`check_errors.py` 全部通过。
新增用例 `test_factory_default_is_silent`：
钉住"出厂自启必须静默"（连 `enable()` 的签名默认值一起断言）——
防止以后有人改一个布尔值，就让每次开机都弹出浏览器窗口。

---

## [0.4.1] - 2026-09-14

### 这一版做了什么

修用户实测反馈的**三处界面问题**（BUG-046）。都属于"不报错、不崩溃，
但看着别扭、甚至用不了"的那一类 —— 全靠**量尺寸**才定位到。

**用户原话：**

> 悬浮窗上边框和下边框过于拥挤，里面内容显示不全，悬浮窗设置界面也需要能用鼠标调整大小

### 修复

| 现象 | 实测 | 修法 |
| --- | --- | --- |
| 工具栏"上下被切" | 固定高 **22 px**，按钮需要 **24 px** | 去掉写死高度，让工具栏按内容自己长高（现在 26 px），并加 `TOOLBAR_PAD_Y` 留白 |
| **「日期 ▾」几乎消失** | 需要 63 px，实际只分到 **1 px**（v0.4.0 加 ✎ 时挤爆的） | **pack 顺序反过来**：按钮先占够，标题吃剩下的 |
| 长标题右边被硬裁 | 控件宽 283、文字需要 540，`wraplength=0` | 画完立刻算一次折行；画布宽度变化（比如滚动条冒出来）时也重算 |
| 设置弹窗拉不动 | `resizable(False, False)` | 弹窗默认允许鼠标调整大小 + `minsize(280, 240)`；滑块跟着变宽、描述框跟着变高 |

### 改进

- **工具栏标题三级自适应**：够宽显示「📝 离线待办清单 12」，中等显示「📝 待办 12」，
  窄了缩成「📝 12」/「📝」—— **不是把信息删掉，而是空间不够时换个更短的表达。**
- **缩放下限按实际需要抬高**：`FLOATING_MIN_WIDTH` 200 → **280**
  （工具栏那 6 个控件本身就要 224 px，200 宽的窗口在任何情况下都塞不下它），
  `FLOATING_MIN_HEIGHT` 160 → **200**。默认窗口是 320，正常使用不受影响。
- 底部输入条的上下留白放宽一点（原来紧贴着输入框和提示行）。

### 验证

| 项 | 修前 | 修后 |
| --- | --- | --- |
| 工具栏高度 | 22（被切） | **26**（= 子控件需要的高度） |
| 「日期 ▾」宽度 | **1 px** | **63 px** |
| 280 px 窄窗口 | 「日期 ▾」被挤成 27 px | 6 个控件全部完整，标题自动缩成「📝」 |
| 长标题 `wraplength` | **0**（被裁） | **214**（正常折行） |
| 设置弹窗 | 拉不动 | 可调大小，最小 280×240 |

全量测试、`check_repo.py`、`check_errors.py` 全部通过。
界面截图见 `data/test/_验收截图/`（`v041_main.png` / `v041_settings_resized.png`）。

---

## [0.4.0] - 2026-09-14

### 这一版做了什么

**一轮完整的验收测试之后的修复版。** 用户要求"逐条验收 + 查 Bug + 查安全"，
结果查出 **12 个真实缺陷**（其中 3 个是"功能不可用"级别），全部在这一版修掉，
并新增一个用户点名要的小功能。

一句话概括这轮发现：**"代码能跑起来不报错"和"功能真的能用"完全是两件事。**
最典型的是日历选择器 —— 它从 v0.3.2 做出来那天起就**一次都没能用过**，
而 CHANGELOG 和待优化清单都写着"已完成"。

### 修复（按严重度）

| 编号 | 一句话 | 影响 |
| --- | --- | --- |
| BUG-033 | 点编辑弹窗里的 📅 立刻抛 `AttributeError`，留下一个关不掉的空盒子 | **功能完全不可用** |
| BUG-034 | 守护线程的"失败退避"在它本该防的场景下**永远不触发** | 无限重启防线失效 |
| BUG-035 | 并发点「立即显示」喷出十几个进程 + 接口返回 500 | 进程堆积 + 报错 |
| BUG-036 | 滚动条被 pack 了却永远是 1×1，用户根本看不到 | 看不到也拖不动 |
| BUG-037 | 字号 ≥13 磅时编辑弹窗底部四个按钮**整排消失** | 无法保存 |
| BUG-038 | 「之后 10-24」排在「之后 9-22」前面；且每个远期日期各成一块 | 分组顺序错 |
| BUG-039 | 设置页改的窗口坐标被拖动回写**静默覆盖** | 修改丢失 |
| BUG-040 | 开机自启脚本里的 `--floating` 到不了 `run.py` | 登录后要多等 5~8 秒 |
| BUG-041 | 搜索框里的 `%` / `_` 被当成 SQL 通配符 | 搜 `%` 命中全部 |
| BUG-042 | 一键配色会把用户调好的不透明度悄悄改掉 | 与刚定的默认相反 |
| BUG-043 | 本机任何网页都能跨站调用一批"不要令牌"的接口（含 `/api/settings/shutdown`） | **能关掉整个程序** |
| BUG-044 | `AGENTS.md` 明令禁止的 `except ...: pass` 仍有 **49 处** | 静默失败 |
| BUG-045 | 有 2 个测试**每天 22:30 之后必然失败** | 测试时段性假红 |

### 新增

- **顶部工具栏的「编辑」按钮（✎）**（用户 2026-09-14 确认）。
  它编辑"最近点过的那一条"——点任务行时会记下是哪条；
  没选过就提示一句"先点一条待办"，**绝不随便挑一条打开**。
  （点任务行直接弹出编辑窗这个原有行为保持不变。）

### 改进

- **跨站请求防护（便宜档）**：新增 `guard_cross_site` 中间件。
  带 `Origin` 且不是本机来源、或 `Sec-Fetch-Site: cross-site`、
  或 `Origin: null` 的请求一律 403。
  命令行、悬浮窗进程（走 urllib、不带这个头）不受影响。
- **「之后」合成一档**（用户确认："一档就可以，但每一项后面表明具体日期"）：
  远期任务只占一个「之后」块，具体日期显示在**每条任务自己**那一行上。
  顺带消灭了"同档位按标题字符串排序导致 10 月排在 9 月前面"的问题。
- **一键配色只管颜色，不再动不透明度**：用户调好的透明度不该因为换个配色被重置。
- **弹窗尺寸随内容自适应**：不再写死 520 / 500 像素，
  字号调到 72 磅时按钮也不会被挤掉。
- **`set_value()` 改成一条 SQL 的 upsert**：消灭"先查再插"的并发竞态。

### 工程质量

- `scripts/check_errors.py` 新增两节自动检查：
  **跨站请求防护**（6 项）和 **"代码里不许有 `except: pass` "**。
  后者是把一条一直只写在文档里的规则**变成了会被自动拦住的检查** ——
  规则不配检查，就只是愿望。
- 8 个文件共 49 处 `except ...: pass` 改成至少记一行日志
  （其中 6 个文件补上了 logger）。
- `tests/test_floating_service.py` 修掉 2 个"每天 22:30 之后必红"的断言，
  并删掉一条**恒真断言**（`assert which in ("today","tomorrow")` —— 它什么都没验证）。

### 测试

- 全项目测试全部通过（在 **22:45** 这个曾必红的时段跑的）。
- `check_repo.py`（35 项）、`check_errors.py`（含新增两节）全部通过。

### 数据安全

- 本轮验收全程使用隔离环境（`TODO_DATA_DIR` 指向 `data/test/` 下的目录），
  **真实数据库 `data/todo.db` 未被动过**（验收前后大小与时间戳一致）。

---

## [0.3.3] - 2026-09-14

### 改了什么

把出厂默认的透明度按用户要求改回 **「黑色背景 + 透明度 30%」**，
同时**纠正了一个理解错误**：原来把「透明度」和「字体清晰度」当成了互相冲突的两件事，
其实它们互不干涉。

### 默认外观

| 项 | 这次之前 | 现在 |
| --- | --- | --- |
| 背景色 | `#000000`（纯黑） | 不变 |
| **不透明度** | 0.92（也就是透明度 8%） | **0.70**（也就是**透明度 30%**） |
| 可调范围 | 80% ~ 100% | **30% ~ 100%**（即透明度 70% ~ 0%） |
| 提醒线 | 85% | **60%** |
| 字体 / 字号 | 宋体 9 磅 | 不变 |

### ★ 纠正了一个理解错误：「透明度」和「清晰度」不是一回事

用户澄清的原话：

> 字体清晰指的是字体分辨率保持高，在任何情况下不能因为模糊像素低而看不清

也就是说：

| | 是什么 | 受透明度影响吗 |
| --- | --- | --- |
| **清晰度（锐利）** | 笔画边缘利不利、有没有糊 | ❌ **不受影响** —— 由 DPI 渲染决定 |
| **对比度（好不好读）** | 字和背景的亮度差 | ✅ 会按比例变小 |

**为什么降低透明度不会让字变糊？** 因为 `-alpha` 走的是 Windows 的
分层窗口机制：整幅位图按同一个系数和桌面混合，每个像素只和**同一位置**的底色混合，
**不涉及邻域平均** —— 而"模糊"的本质正是邻域平均。

**所以上一个版本（0.3.1）把透明度下限抬到 80% 的那次取舍是白做的**，
它建立在一个错误前提上。现在：
- 「字必须清晰」这条约束落在 **DPI 感知**上（0.3.1 已做）；
- 「透明度可以调低」这条要求落在**范围**上（这次放开）。

两条要求根本不冲突。这也解释了设置页文案为什么要重写：
不能说"调低会发虚"（那是错的），要说"桌面会透进来、字和背景的差别变小、变难读"。

### 顺带纠正：默认透明度的方向曾经写反

用户说「透明度 30%」时，代码里应该写**不透明度 0.70**（允许三成的光透过去）。
第一版曾经直接写成 `opacity = 0.30` —— 那等于**透明度 70%**，几乎是一层全透的玻璃。
现在用三重措施钉住这个方向：

1. 配置注释里把三种说法**并排列出**（0.70 ↔ 30%、1.00 ↔ 0%、0.30 ↔ 70%）；
2. 测试里连**方向**一起断言，并在断言消息里写明"写成 0.30 就是理解反了"；
3. 设置页滑块标签写成「背景不透明度（透明度 = 100% − 这个数）」，
   并在旁边**实时显示换算结果**（调的时候能直接看到"透明度 30%"）。

### 文档

- `README.md`：「关于透明度和字体清晰」一节**重写** ——
  改成先讲清"不透明度 / 透明度"两个方向相反的词，再讲"清晰度 / 对比度"的区别。
- `docs/阶段四-01-桌面悬浮窗.md`：新增「不透明度和透明度方向相反」与
  「清晰度和对比度是两件事」两节；把 0.3.1 那次被推翻的取舍**保留原样并标注推翻了**，
  当作"基于错误前提做取舍"的活教材。

### 测试

- 全项目 **499 项**测试全部通过（v0.3.2 是 492 项）。
- 有两条测试专门守着这次的理解：
  - `test_default_look_black_30_percent_transparent_songti`（方向别写反）；
  - `test_clarity_and_transparency_are_different_things`（两个概念别混）。

### 修复

- **BUG-032：6 个测试"只在一天的某个时段"才通过。**
  它们把"未来时间"写死成 `今天 23:00` / `今天 20:00`，
  而业务规则禁止"截止时间早于当前时间" —— 于是**过了那个点跑测试就会假失败**。
  这个缺陷是晚上 20:02 修别的东西时撞出来的（正好有个测试用了"今天 20:00"）。

  **危害在于：「偶尔红一次」的测试会训练你忽略红色** ——
  一旦养成"红了大概是巧合、再跑一遍就好"的习惯，真正的问题也会被这样糊过去。

  改法：
  1. 在 `tests/conftest.py` 里加了两个"算时间"的工具
     （`future_due()` / `future_due_today()`），把 6 处写死的时间全部换掉；
  2. 深夜实在没有合法时刻时**诚实地 skip**，而不是硬凑一个过去的时间；
  3. 给这两个工具补了 **6 项单元测试**，用注入的"假时钟"
     把一天 24 小时挨个跑一遍 —— 这种缺陷**没法靠"跑一遍测试"发现**，
     因为跑的时候时钟是固定的，必须能把"现在几点"注入进去。

---

## [0.3.2] - 2026-09-14

### 这一版做了什么

把 0.3.1 留下的**已知问题**清掉了三个（OPT-025 / 027 / 029），
并把两件"约定"落进了文档（多进程日志初始化、改文档的转义纪律）。
不新增面向用户的大功能，主要是**让已有的东西更可靠、更好用**。

### 新增

- **悬浮窗的日历选择器**（OPT-029）。
  截止时间和提醒时间旁边各有一个 **📅** 按钮，点开是一个小日历：
  可以翻月、点日期，底下一排快捷键「今天 / 明天 / 下周一 / 清空」，
  还有一个独立的时间输入框。
  **只改日期时会保留原来的时刻** —— 不用把 "18:00" 重新打一遍。
  （以前只能手打；虽然做了"只打 20:00 也能用"的补全，
  但"下周三到底是几号"这种换算还是得用户自己算。）
- **编辑弹窗里的「＋ 新分类」**（OPT-027）。
  分类栏现在**永远**显示这一行快捷按钮：一个「＋ 新分类」+
  最近用过的 5 个分类。点「＋ 新分类」会清空输入框并聚焦，
  打个新名字保存就记住了。
  （以前第一次用某个分类时，那个分类当然不在列表里，
  用户会以为"只能选已有的"。）

### 改进

- **守护线程加了"失败退避"刹车**（OPT-025，由 BUG-027 复盘而来）。
  以前窗口如果因为配置问题反复启动失败，守护线程每 3 秒就再拉一个，
  变成**无限重启 + 进程堆积 + 日志刷屏**。现在分三层：

  | 情况 | 行为 |
  | --- | --- |
  | 连续失败 3 次 | 检查间隔从 3 秒放慢到 30 秒（少刷日志、少拉进程） |
  | 连续失败 10 次 | **停下自动重试**，并在设置页显示"原因 + 该怎么办" |
  | 用户点「立即显示」/「重启窗口」/ 改设置 | 立刻解除退避，重新开始试 |

  设置页的悬浮窗那一块新增了「**自动重试状态**」一行 —— 正常时隐藏，
  出问题才显示出来（因为"程序不再尝试拉起窗口"这件事本身不弹窗、不报错，
  不告诉用户，他只会觉得这功能坏了）。

### 文档与约定

- **`AGENTS.md` 新增「多进程约定」**：以后每新增一个独立进程，
  必须在入口最前面调 `logging_setup.setup()`（否则它的日志会静默消失，
  见 BUG-030），并且绝不让新进程直连数据库。
- **`AGENTS.md` 新增「改文档 / 改代码文本的纪律」**（BUG-031 的教训）：
  改 Markdown 一律用 `edit` 工具按字面替换，**不要用 PowerShell 的字符串替换** ——
  PowerShell 双引号里的转义符是反引号，而 Markdown 到处都用反引号标代码，
  两者一撞就会把文档悄悄改坏，而且**任何自动化检查都不会报错**。
- **`README.md` 补齐了「桌面悬浮窗」章节**（给使用者看的）：
  怎么开、怎么用、外观怎么调、窗口不见了怎么办，
  以及单独一节解释「为什么透明度不能调得更低」。

### 修复

- **BUG-031**：用 PowerShell 批量改 Markdown 时，反引号被当成转义符吃掉，
  把索引表里的一行改成了乱码。已修好，并写了"文档体检"命令
  （扫 `.md` 里的控制字符）确认其它文档没被波及。

### 测试

- 全项目 **499 项**测试全部通过（v0.3.1 是 481 项）。
- 新增 11 项测试集中在守护线程的退避逻辑上，包括：
  连续失败 3 次进入退避、10 次放弃、**成功一次就清零**、
  用户手动重试能解除、关掉功能时顺手清状态、接口暴露了健康状况。

### 仍然做不到的（如实记录）

- **逐控件透明**（只让背景半透明、文字始终锐利）：tkinter 的 `-alpha`
  作用在整个窗口上，这是它的能力边界。用"下限 80% + 默认 92%"把
  后果控制住了，但要根治得换 GUI 框架（与"不引入不必要依赖"冲突）。
- **多显示器各自记忆窗口位置**（只记住一组坐标）。
- 日历面板是自绘的，没有年份快速跳转、也没有键盘操作（OPT-031）。

---

## [0.3.1] - 2026-09-14

### 改版说明

0.3.0 的悬浮窗在真机上用了一会儿之后，根据实际反馈做了一轮**界面重做**。
这一版不新增功能，而是把"用起来别扭"的地方改顺，并**明确了优先级**。

### 界面重做（按用户反馈逐条对照）

- **窗口层级改了：悬浮窗在桌面层，任何窗口都能覆盖它。**
  用户原话：「悬浮窗要设于桌面，任何窗口都能覆盖」。
  也就是说它的定位从"压在所有软件上面的浮层"改成了"贴在桌面上的便签" ——
  你打开浏览器工作时它会被挡在后面，按 Win+D 回到桌面时它就在那里。
  （默认不再"总在最前"；想改回来：右键标题栏勾「总在最前面」，或 ⚙ 里打开。）
- **编辑和分类搬到上方**，底部只留一个"快速添加"输入框。
- **编辑改成完整弹窗**（原来只有 4 个字段，被评价为"功能过于简单"）：
  标题 / 分类（带已有分类快捷按钮）/ 重要度 / 截止时间 /
  提醒时间（带"10 分钟后""今天 18:00""明天 09:00"快捷项）/ 详细描述，
  外加「删除」和「标记完成」两个动作。
  **点一行的任意位置**都能打开它（原来只有标题文字可点，右半行是空的）。
- **分组标题带上具体日期**：`今天 9-14`、`明天 9-15`、
  `七天内 9-16 ~ 9-21`（这一档显示的是**范围**，因为它装了好几天）。
  档位也重划成：已过期 → 今天 → 明天 → 七天内 → 之后 → 未安排日期。
- **分类切换按钮带三角**：顶部 `[日期 ▾]`，点一下即可在
  「按日期 / 按重要度（优先级）/ 按待办类型（分类）」之间切换。
- **排版收紧**：默认字号 10 → **9 磅**，行距边距一起收；
  滚动条改成**需要时才出现**（内容放得下就不占地方）；
  放不下时鼠标滚轮可下拉。
- **鼠标移到窗口边缘即可移动 / 缩放**：四条边 + 四个角都是感应区，
  光标会变成对应的箭头（↔ ↕ ⤡）；拖角改两个方向。
  **改完大小后文字会重新折行**（不会出现"变宽了但文字还在中间断行"）。
- **右上角新增 ⚙ 设置按钮**，弹窗里就是用户点名的那几项：
  字号 / 背景颜色 / 字体 / 字体颜色 / 透明度 / 是否关闭悬浮窗。
  ⚙ 里的「关闭悬浮窗」和标题栏的 ✕ **不是一回事**：
  ✕ 只是收起来（下次开机还会出现），「关闭悬浮窗」是真正关掉功能。

### ★ 字体清晰度优先（用户明确要求）

用户原话：「**保持字体的高清晰度为排版的最基本前提**」。
这条一出来，就给"想要很透"和"想要字清楚"这对冲突定了优先级 ——
**清晰优先**。所以：

- **声明 DPI 感知**（新增 `enable_dpi_awareness()`，按系统版本依次尝试三个 Win32 API）。
  不声明的话，Windows 会把整个窗口**拉伸放大**，文字被插值糊掉 ——
  这是"看起来发虚"最容易被忽略、影响却最大的一条原因。
- **透明度默认 30% → 92%，下限 5% → 80%**：
  整窗透明度会把文字一起变淡，低于 80% 字就开始明显发虚，所以**不允许**调更低。
  0.85 处界面上会提醒"字开始发虚了"并解释原因。
- 设置页和文档里都写清了"为什么不能再低" ——
  让用户知道这不是软件做不好，而是"整窗透明"这个做法本身的物理后果。

### 修复

- **BUG-028**：窗口起来是**一片空白**。原因是把 `padx=(6,2)` 这种
  "左右/上下"的两元素写法用在了**不支持它的控件**上
  （只有 `pack` / `grid` 才支持），Tcl 报 `bad screen distance "6 2"`，
  整个任务列表一条都画不出来。同时去掉了一处 `except: pass` ——
  正是它把这个错误伪装成了"界面是空的"。
- **BUG-029**：手打时间的"体贴补全"是**按字符串长度和位置猜**的，三个漏洞：
  `9-16 09:00` 被漏判、`25:00` 被硬拼成一个解析不了的假时间、
  `2026-9-6` 补出来的月份没补零。改成**正则精确匹配形状 + 回环校验**。
- **BUG-030**：① 悬浮窗进程**没有配置日志**，所有 `logger` 调用被静默丢弃
  （异常 → lastResort → 写 stderr → 而 `noconsole` 下 stderr 是空的），
  窗口崩了只剩一行无关的话可看。② 没声明 DPI 感知导致文字发虚（见上）。
  另外新增一行"界面已应用：bg=… 字体=… 几何=… 置顶=…"的自述日志 ——
  它能在排查时一眼区分"设置没生效"和"窗口被别的窗口盖住了"。

### 顺带修好的老问题

- **OPT-024**（从设置页改窗口坐标不立刻生效）：重做界面时把"应用几何"
  统一到一个函数、位置和尺寸各管各的，实测**已经生效**。
  现在改位置的延迟上限是轮询间隔 3 秒（设计如此）；窗口自己拖动是立即的。

### 测试

- 全项目 **481 项**测试全部通过（v0.3.0 是 450 项）。
- 新增的测试里有两类特别值得一提：
  - `TestNormalizeDatetime`：**先写测试才发现 BUG-029 的三个漏洞** ——
    其中两条在修复前就是红的。这就是"先写测试"的直接收益。
  - `TestRelayoutOnResize` / `TestResizeEdges`：验证"改大小后重新折行"
    和"边缘感应区的锚点算得对"（锚点算错会让感应条跑到窗口外面去）。

### 已知限制

1. **透明度是"整窗"的，不是"只让背景透明"**。技术限制没有变
   （tkinter 的 `-alpha` 作用于整个窗口），但通过"抬高下限 + 默认 92%"
   已经不会造成"用户看到糊字"这个后果。详见 `docs/待优化与已知问题.md` 的 OPT-026。
2. 编辑弹窗里的时间是**手打文本**，还没有日历选择器（OPT-029）。
3. 悬浮窗里不能新建一个全新分类，只能手打分类名（OPT-027）。

---

## [0.3.0] - 2026-09-14

### 新增：桌面悬浮窗（本次的主要改动）

一个一直浮在桌面上的小窗口，直接显示待办。
**关掉浏览器它也在**，可以自由拖动位置、拖右下角改大小，
还能调透明度、字号、颜色和字体。

- **自由移动 / 控制大小**：按住标题栏拖动，拖右下角 ◢ 改大小。
  位置和大小都会自动记住，下次打开还在原地。
- **背景透明度**：5% ~ 100% 可调（默认 **30%**）。
  ⚠️ 这是**整窗**透明度（文字会跟着变淡），不是只让背景透明 ——
  这是 tkinter 的能力边界，详见下方"已知限制"。
- **字号 / 颜色 / 字体**：字号 6 ~ 72 磅；
  文字色 / 背景色 / 强调色三个取色器；
  字体下拉框预设 **宋体 / 楷体 / 仿宋 / 黑体** / 微软雅黑 / Consolas / Segoe UI，
  也可以手打别的字体名（没装会自动回退，不会出错）。
- **一键配色主题**：黑玻璃 / 纸白 / 墨黑 / 护眼绿 / 暖茶 / 幽灵。
- **设置提醒**：在窗口里新建待办时可直接选「10 / 30 / 60 分钟后」、
  「今天 18:00」、「明天 09:00」；到点由后台线程发 Windows 系统通知，
  窗口内同时浮出提示条（双保险）。
- **编辑内容**：点一行文字即可改标题、优先级、分类、提醒时间；
  点方框（或双击）切换完成状态；右键有「稍后提醒」「删除」。
- **分块显示**：可按**日期**（已过期 / 今天 / 明天 / 本周稍后 / 更远 / 未安排日期）、
  按**优先级**（高 / 中 / 低）、按**分类**分块；
  另有「显示范围」可选 未完成 / 今天 / 未来七天 / 全部。
- **在设置里开关**：默认**关闭**（这是个会开机自启的功能，
  默认打开会让用户第二天开机时莫名多出一个窗口）。关掉开关窗口即消失。
- **开机自启后自动出现**：设置里可勾选「开机自启时也显示悬浮窗」，
  登录 Windows 后窗口会自己出现，可直接编辑和收提醒。
  服务端有守护线程盯着，窗口意外退出会自动被拉回来。

### 出厂默认外观

| 项 | 默认值 |
| --- | --- |
| 背景色 | `#000000`（纯黑） |
| 文字色 | `#FFFFFF`（白色） |
| 强调色 | `#FFD166`（暖黄） |
| 透明度 | **30%** |
| 字体 | **宋体** |
| 字号 | 10 磅 |
| 位置 / 大小 | (80, 80) / 320 × 420 |

### 新增的接口

```
GET    /api/floating                    全部设置与运行状态
PATCH  /api/floating/config             改设置（只传要改的字段）
POST   /api/floating/theme              一键套用配色
POST   /api/floating/reset              恢复默认设置（只重置外观位置，不碰数据）
POST   /api/floating/window/show        立即显示
POST   /api/floating/window/restart     重启窗口
POST   /api/floating/window/close       隐藏窗口
GET    /api/floating/data               窗口要显示的数据（分块后的，需令牌）
GET    /api/floating/config             窗口轮询设置（需令牌）
GET    /api/floating/remind-at          提醒快捷选项换算（需令牌）
POST   /api/floating/tasks              在窗口里新建待办（需令牌）
POST   /api/floating/tasks/{id}/toggle  勾选 / 取消勾选（需令牌）
PATCH  /api/floating/tasks/{id}         在窗口里编辑（需令牌）
POST   /api/floating/tasks/{id}/snooze  稍后提醒（需令牌）
```

### 新增的命令行参数

- `python run.py --floating`：只启动悬浮窗（不起服务）
- `python run.py --floating-stop`：让正在运行的悬浮窗退出

### 技术说明（不关心实现的可以跳过）

- **界面用 Python 标准库 tkinter 画**，**没有引入任何新依赖**。
  代价是界面风格朴素（想更漂亮得自己画）。
- **窗口是独立进程**，不是服务的一个线程 ——
  因为 tkinter 的界面必须在主线程跑 `mainloop()`，而服务的主线程被 uvicorn 占着；
  顺带还换来"窗口卡住不拖累服务""服务重启窗口不死"两个好处。
- **窗口通过本机 HTTP 读数据**，不直连数据库
  （避免两个进程同时写库，也避免把数据层代码维护两份）。
- **打包配置改动**：`OfflineTodoList.spec` 里去掉了
  `--exclude-module tkinter`，并加了 `app.gui.window` 作为 hidden-import。
  **代价是安装包变大（约 +10 MB），换来的是安装版也能用悬浮窗。**
- **新增一次数据迁移**：开发期间"透明度"曾用百分比整数（30~100）存储，
  现在统一为小数（0.05~1.0）。服务启动时会把旧值自动换算
  （92 → 0.92），**幂等、可重复执行、容忍坏值**。
- **窗口令牌**：能读写数据的 `/api/floating/*` 接口要求带 `X-Float-Token` 头，
  令牌由服务端生成、通过环境变量传给窗口进程。
  这不是完整的鉴权（服务只监听 127.0.0.1），作用是"防误触、防别的网页瞎调"。
- **「不会开出两个窗口」用两道闸门**：服务端线程锁 + 窗口进程的文件锁。
  文件锁在进程被杀时由操作系统自动释放，比 pid 文件可靠。

### 测试

- 新增 4 个测试文件、**191 项**新测试，全项目 **450 项**全部通过。
- 其中包含两条"防复发"的自动检查：
  - 检查"所有设置项是否都被窗口的变化监听覆盖"（BUG-026 的教训）；
  - 检查"悬浮窗代码里有没有绕过唯一网络出口直接发请求"。

### 修复

- **BUG-026**：窗口"监听设置变化"的字段清单里漏了 `width` / `height`，
  导致改窗口大小毫无反应，要等再改一次透明度才顺带生效。
- **BUG-027**：启动悬浮窗时忘了把访问令牌传给子进程，
  窗口每次请求都被 403 拒绝而退出，守护线程又把它拉起来 ——
  **无限重启**，任务管理器里堆出一串 `pythonw`。
- **版本号漏改**：`installer/install.py` 的 `APP_VERSION` 还停在 `0.2.0`，
  核对时发现并改为 `0.3.0`（这正是 `AGENTS.md` 里那条核对约定要防的事）。

### 已知限制

1. **透明度的作用范围是「整个窗口」**，不是只让背景透明。
   也就是说调低透明度会让文字一起变淡。
   要做到"只有背景半透明、文字清晰"需要逐控件设置，
   而 tkinter 没有这个能力（这是它的能力边界，不是实现偷懒）。
   现在的缓解办法是把下限设在 5%（而不是 0），并配合黑底白字。
2. **从设置页改窗口坐标（x / y）可能不会立刻生效**（改大小、透明度、颜色都正常）。
   关掉窗口重开、或点「重启窗口」后位置是对的。
   详见 `docs/待优化与已知问题.md` 的 **OPT-024**。
3. 悬浮窗**不出现在任务栏和 Alt+Tab 里**（这是有意设计的：
   它的正确形态是"贴在桌面上的便签"）。想找到它请用设置页的「立即显示」。
4. 非 Windows 系统上，开机自启不可用（沿用原有行为的限制）。

---

## [未发布]

### 已完成（工程与仓库整理）

这一批改动不影响使用者看到的功能，但让项目更容易维护和交接。

- **仓库根目录确定为 `win11/`**：`.git` 从上层目录移入本项目，
  这样只有本项目会被推送到 GitHub，同级的其它版本目录完全留在本地。
- **`.docx` 不再进入 Git**：Word 版是 Markdown 的派生产物，
  二进制文件每次重新生成都会产生巨大且无意义的 diff。
  想重建全部 Word，跑一条命令即可（见下）。
- **新增 `python scripts/md2docx.py --all`**：一次生成全部 Word 文档，
  不必逐个敲文件名。（单文件用法保持不变）
- **清理仓库冗余对象**：`git gc` 使 `.git` 从 1.87 MB 降到 0.39 MB。
- **新增「上下文压缩与交接」流程约定**（`AGENTS.md` 第 5 节）：
  对话变长时先落盘再继续，避免靠记忆硬撑。
- **修正提交信息笔误**：`first vide coding` → `first vibe coding`。

### 已完成（安装器 · 覆盖升级）

- **可以直接覆盖安装新版本，不需要先卸载。**
  双击新版的安装包、点「开始安装」即可 —— 程序文件、快捷方式、
  以及「应用和功能」里的版本记录都会自动替换，不会出现重复项。
- **升级不会影响你的待办数据**：程序装在 `%LOCALAPPDATA%\Programs\OfflineTodoList`，
  数据存在 `%LOCALAPPDATA%\OfflineTodoList\data`，两者完全分开存放。
- **安装界面会显示升级信息**：检测到已安装的版本、将升级到哪一版，
  并说明数据存放位置。（原先只显示"点按钮开始安装"，用户不知道会发生什么）
- **替换方式改为「原子替换」**：原先的"先删旧的、再复制新的"一旦复制失败，
  用户就会两头空。现在改成"先备好新版、再整目录替换"，
  失败时**旧版本完好无损**，并且支持回滚。
- **修正安装器里写错的版本号**：安装界面原先显示 0.1.0，而应用实际已是 0.2.0。
  安装界面显示的版本是用户判断「我装的是不是新版」的第一依据，写错会直接误导人。

### 已完成（README 措辞专业化）

- README 全文改为**说明性语言**：去掉一问一答式的问句标题、emoji 标记、
  第二人称与箭头串联步骤，统一为陈述句。
- 末尾三个 `<details>` 问答折叠块改为平铺的说明性章节
  （数据存放位置 / 删除与恢复 / 系统支持），并修掉一个孤立的 `</details>` 标签。
- **修正一处事实性错误**：原文写「删除后无法恢复」，实际 v0.2.0 已实现回收站。
- 功能特性新增「回收站」一节。

### 计划新增
- 前端网络断开时的友好错误提示（OPT-013）。
- 「撤销」按钮：删除后几秒内可一键撤回，不用专门去回收站（OPT-001 的体验延伸）。

### 计划变更
- 把前端 DOM 交叉检查固化为通用脚本 `scripts/check_frontend.py`（OPT-017，目前在回收站页已有单页版本）。
- 继续拆分过长函数：`scripts/md2docx.py`、`scripts/clean.py`。

---

## [0.2.0] - 2026-09-13

**本次的重点是「数据安全」** —— 堵上项目里风险最高的一处缺口：误删无法找回。

### 新增

**回收站（软删除）**
- 删除不再真正销毁数据，而是移入回收站，**随时可以还原**
- 新增「回收站」页面：查看已删除任务、还原、彻底删除、清空回收站
- 彻底删除与清空回收站都有**二次确认**，且只能作用于已在回收站里的任务
- 已删除的任务不会出现在列表、搜索、统计、日历里，也**不会再弹提醒**
- 列表页的删除提示改为「会先被放进回收站」，不再吓唬用户

**数据库自动升级**
- 老版本创建的数据库会在启动时**自动补上新字段**，不需要用户做任何事
- 升级只做「加列」，不触碰任何已有数据；同时内置幂等保护，重复启动不会出错

### 修复

- **BUG-019**：脚本输出被重定向到文件时崩溃。
  输出里的 `✅` 在 GBK 编码下无法表示，直接抛 `UnicodeEncodeError`；
  更糟的是它崩在最后一行，导致「前面全跑完了，结果一个字都没写进文件」。
  已改为纯文本标记，并新增 `scripts/_console.py` 作为通用输出保护。
- **BUG-020**：某个脚本里硬编码了开发者本机的绝对路径（形如 `X:\...\to_do_list`）。
  会导致项目换台电脑就失效，还把本机目录结构泄露到了公开仓库。已改为从文件位置推算。
- **BUG-021**：自检脚本跑第二遍必然误报失败。
  日志是跨次运行累积的，而断言在数日志里的行数，于是越跑错得越多。
  已在导入应用前清空隔离日志，现在连跑三次都是「全部通过」。
- **BUG-018**：时间解析过于宽松，`2026-9-13 8:30` 这种没补零的格式也会被接受，
  导致非规范字符串进库、破坏「字符串排序即时间排序」的前提。已加入往返校验。

### 变更

- 版本号 0.1.0 → 0.2.0
- 自检工具与测试脚本统一使用输出保护，避免编码问题再次打断检查
- 回收站页面使用 DOM API 而非 `innerHTML` 渲染，从原理上杜绝 XSS

### 测试

- 测试总数 229 → **259 项，全部通过**
- 新增 `tests/test_trash.py`，覆盖数据库迁移、软删除语义、四个新接口、路由顺序回归等

### 安全

- 真实数据库的升级流程已在**副本**上做过完整演练：14 列 → 15 列，数据条数与内容均无变化
- 本版本关闭了「误删即永久丢失」这一风险点

### 打包

- 已重新打包并完成端到端验证：免安装版可直接运行、安装版可安装/运行/卸载
- 安装包体积 32.6 MB，免安装版 9.7 MB

---

---

## [0.1.0] - 2026-09-13

第一个可用版本。**离线待办清单** —— 本地优先，断网可用，数据只存在自己电脑上。

### 新增

**待办管理**
- 添加 / 查看 / 编辑 / 删除待办
- 标记完成与取消完成，完成时间自动记录
- 按状态筛选：全部 / 未完成 / 已完成
- 关键字搜索（同时匹配标题、描述、分类）
- 优先级（低 / 中 / 高）、分类、详细描述、截止时间
- 完成情况统计

**提醒系统**
- 提醒时间支持**分钟级**精度（`YYYY-MM-DD HH:MM`）
- 后台守护线程每 20 秒扫描一次到点任务
- **防重复提醒**：靠数据库里的 `reminded_at` 字段保证只弹一次，
  程序重启不会重弹，关机期间到点的提醒开机后也能补上
- **Windows 原生通知**：右下角弹出、不点不走，浏览器关掉也能收到
- 网页内右下角常驻提醒卡片，支持「完成」和「稍后提醒」
- 可选提示音开关

**日历**
- 月历视图，有任务的日期会标出数量
- 点击未来任意一天即可新建当天的任务
- 查看某一天的任务明细

**设置**
- 开机自启开关（可同时选择是否自动打开浏览器）
- 手动备份数据库、一键打开数据文件夹
- 在线查看运行日志、手动注册通知身份
- 测试通知按钮（一键验证提醒能不能弹）

**工程化**
- 三层架构：`routers` → `services` → `repositories`
- 统一异常体系与全局异常处理
- 文件日志（按天轮转，保留 14 天）+ 未捕获异常自动记录
- 启动时自动备份数据库，保留最近 7 份
- 229 项自动化测试（pytest），测试全程在隔离数据库中进行
- 自检脚本：数据层自检 34 项、异常处理自检 30+ 项、冒烟测试
- 一键打包：`build.bat` 生成免安装版，`build_installer.bat` 生成安装程序
- 自带安装器（不依赖 Inno Setup / NSIS），支持静默安装与卸载
- 双击即用的 `.bat` 入口：启动 / 自检 / 清理 / 打包 / 提醒测试

### 变更

- **编辑界面改为弹窗**：原先就地填回顶部表单，不够突出、容易看漏（用户反馈）。
- **提醒升级为系统级通知**：原先只在网页内提示，关掉浏览器就收不到；
  现在由后端线程直接调用 Windows 系统通知（用户反馈）。
- 时间解析增加**往返校验**：只接受严格补零的格式，防止非规范字符串污染数据库、
  破坏「字符串排序即时间排序」这一前提（见 BUG-018）。
- 通知身份默认使用「自动」模式：优先用系统已注册的身份，避免通知被 Windows 静默丢弃。
- 依赖精简：提醒调度改用标准库 `threading`，通知改用系统自带 PowerShell，
  **运行时依赖只有 4 个**（fastapi、uvicorn、sqlalchemy、pydantic）。

### 修复

**启动与环境**
- 启动脚本双击后窗口一闪就退（BUG-001）
- 输出被重定向时启动横幅不可见（BUG-010）
- 模块文档字符串里的非法转义序列（BUG-017）

**前端**
- 页面残留对已删除 DOM 元素的引用（BUG-008、BUG-015）

**后端**
- `/api/tasks/stats` 被 `/api/tasks/{task_id}` 抢先匹配导致参数校验失败（BUG-006）
- 异常类继承顺序错误导致启动即报错（BUG-007）
- 日历接口对非法年月参数直接返回 422，容错逻辑来不及生效（BUG-009）
- 时间解析过于宽松，接受了没补零的非规范格式（BUG-018）

**提醒**
- 后台线程因引用已改名的常量而静默死亡，提醒整个失效（BUG-012）
- Windows 因通知身份未注册而**静默丢弃**所有通知（BUG-013）
- 提示音被错误地当作「通知已送达」而被关闭（BUG-014）

**文档与工具**
- 文档里的转义字符残留、日期写错、用户找不到脚本目录（BUG-002、BUG-003、BUG-004）
- 测试数据库被写进系统临时目录，违反项目自包含原则（BUG-005）
- 提醒测试脚本的菜单死循环，批量创建了 108 条多余任务（BUG-016）

### 安全

- **修复一次数据丢失事故（BUG-011）**：一个临时测试脚本误操作了真实数据库，
  导致用户手动创建的待办全部丢失且无法恢复。由此确立并落实了三项机制：
  1. **测试隔离**：任何写数据的脚本必须先切换到 `data/test/` 下的隔离数据库，
     并在导入应用后二次校验（`scripts/_isolation.py`）。
  2. **危险操作需显式确认**：会清空数据的函数必须传 `confirm=True`，否则抛异常。
  3. **自动备份**：程序每次启动自动备份数据库，保留最近 7 份。
- `.gitignore` 排除整个 `data/` 目录，确保**私人待办数据永远不会被上传到 GitHub**。

---

## 版本说明

- 本文件记录的是**面向使用者**的变更。
- **详细的 Bug 分析**（症状 / 复现步骤 / 报错原文 / 根因 / 修复 / 验证 / 经验教训）
  见 `docs/问题记录与解决方案.md`。
- **待优化清单**见 `docs/待优化与已知问题.md`。
- **项目当前进度与架构说明**见 `PROJECT.md`。
