# 开发/文档依赖（使用者不需要装）
# 安装命令：pip install -r requirements-dev.txt

-r requirements.txt
python-docx>=1.1    # scripts/md2docx.py 用它把 Markdown 生成 Word 阶段文档
pytest>=8.0         # 第三阶段写自动化测试用
pyinstaller>=6.0    # 打包成 exe / 安装包（build.bat、build_installer.bat 用它）
