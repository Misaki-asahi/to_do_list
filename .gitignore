# ==========================================================================
#  .gitignore -- 告诉 Git 哪些文件不要上传到 GitHub
#
#  判断标准只有一条：
#      这是"运行/开发产生的文件"，还是"项目本身的源代码"？
#      前者一律忽略，后者必须提交。
# ==========================================================================

# ---------- 一、数据（最重要！绝对不能上传）----------
# data/ 里全部是运行产生的：数据库、WAL 日志、自动备份、测试库、运行日志、导出文件
# 上传它 = 把你的私人待办公开到 GitHub 上
data/**
!data/.gitkeep

# ---------- 二、Python 自动生成的缓存（可随时删除，用 clean.bat 清理）----------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ---------- 三、测试与覆盖率报告 ----------
.pytest_cache/
.coverage
htmlcov/

# ---------- 四、编辑器与操作系统 ----------
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# ---------- 五、日志 ----------
logs/
*.log

# ---------- 六、Word 文档（.docx）----------
# docs/ 下的每个 .md 都能生成同名 .docx，也可以一次全生成：
#     python scripts/md2docx.py --all
#
# 为什么不提交它们？
#   1. 它们是【派生产物】—— 和 build/、dist/ 同理：源码上传，产物不上传。
#      别人 clone 下来跑一遍上面那条命令就能全部生成；
#   2. .docx 是【二进制】文件，每次重新生成都显示成"整个文件都变了"，
#      改 3 个字 = 一个几万行的 diff，Git 历史会完全没法看；
#   3. 这 15 个文件共 0.73 MB，且 100% 可以从 .md 重新生成。
*.docx

# Word 打开文档时产生的临时文件（以 ~$ 开头）
~$*.docx

# ---------- 七、打包产物（用 build.bat / build_installer.bat 生成）----------
# 这些是"编译出来的东西"，不是源代码 —— 而且加起来 60MB 以上
# 别人 clone 下来自己跑一遍 build.bat 就能生成，没必要上传
build/
dist/
*.spec

# ---------- 八、安装程序构建时的中间文件 ----------
installer/payload/
installer/__pycache__/
