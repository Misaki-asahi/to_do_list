# 项目约定（给协作者 / AI 助手看）

本项目是「离线待办清单」，一个从零学习用的完整项目。以下约定**必须遵守**。

## 1. 文档产出约定

| 时机 | 产出 |
| --- | --- |
| 每个阶段 / 每个功能完成 | `docs/阶段X-YY-名称.md` + 用 `scripts/md2docx.py` 生成同名 `.docx` |
| **每遇到一个 Bug / 报错 / 踩坑** | **在 `docs/问题记录与解决方案.md` 追加一条 `BUG-XXX`，同步更新索引表，并重新生成 `.docx`** |
| **发现"功能上想改但现在先不做"的点** | **记入 `docs/待优化与已知问题.md` 的 `OPT-XXX`，用户反馈要原话照录** |

### ★ `.docx` 不进 Git（重要）

`.md` **要提交**，`.docx` **不提交**（已在 `.gitignore` 里用 `*.docx` 排除）。

**为什么？** 和 `build/`、`dist/` 是同一个道理 ——
`.docx` 是由 `.md` 用脚本生成的**派生产物**：

1. 别人 clone 下来跑一条命令就能全部重新生成，没必要占仓库体积；
2. `.docx` 是**二进制**文件，每次重新生成都显示成"整个文件都变了"，
   改 3 个字 = 一个几万行的 diff，Git 历史会完全没法看。

**生成全部 Word 文档**（在项目根目录执行）：

    python scripts/md2docx.py --all

改了某个 `.md` 之后，也要跑一次 `--all` 让对应的 `.docx` 跟上 ——
**虽然它不上传，但它是交付物的一部分，不能是旧的。**

## 2. Bug 记录格式（七段式，缺一不可）

```
## BUG-XXX：一句话描述
- 日期 / 所属阶段 / 涉及文件 / 状态

### X.1 症状      我看到什么现象
### X.2 复现      什么命令能稳定重现
### X.3 报错原文  原样粘贴，不要翻译
### X.4 根因分析  讲到机制层面
### X.5 修复方案  改了什么、为什么
### X.6 验证      怎么证明真的修好了
### X.7 经验      下次如何避免、能否推广
```

## 3. 代码约定

- 三层架构：`routers`（收请求）→ `services`（业务规则）→ `repositories`（唯一碰数据库的地方）。
  **依赖只能从上往下**，数据层绝不能反向调用服务层。
- 所有路径 / 端口 / 常量只允许写在 `app/config.py`。
- 前端所有网络请求必须走 `app/static/js/api.js`，页面里禁止直接写 `fetch`。
- 时间统一用 `YYYY-MM-DD HH:MM` 本地时间字符串（决策 3B），转换逻辑集中在 `app/core/timeutil.py`。
- **`.bat` / `.cmd` / `.vbs` 文件必须使用 CRLF 行尾，且内容为纯 ASCII**（见 BUG-001）。

## 3.0 ★★★ 数据安全铁律（最高优先级，违反必出事）

**这是用一次真实的数据丢失事故换来的规定（见 BUG-011），任何情况下不得违反。**

### 铁律一：任何写数据的脚本必须使用隔离数据库

在脚本最上面（**必须**在 `from app import ...` 之前）：

```python
from scripts._isolation import use_isolated_db, assert_isolated
TEST_DB = use_isolated_db("脚本名")      # 切到 data/test/脚本名.db
from app import config, database          # 之后才能导入 app
assert_isolated()                         # 第二道网：不是隔离库就立刻中止
```

**"这只是临时脚本"不是理由。** 临时脚本造成的损失是永久的。

### 铁律二：危险操作必须有显式确认参数

会删除/清空数据的函数，一律加 `confirm=False` 参数并在未确认时抛异常。

### 铁律三：不做可能丢数据的功能，必须【最先做】

不要给这类功能标个"高优先级"然后排到最后。**标注优先级 ≠ 执行优先级。**

### 铁律四：永远不要替用户判断"这些数据不重要"

**你觉得数据库是空的，不代表它是空的。** 用户随时可能已经录入了真实内容。

### 铁律五：改动数据前先备份

`POST /api/settings/backup` 或 `backup.make_backup()`。程序启动时也会自动备份。

---

## 3.1 项目自包含原则（重要）

**所有"运行 / 开发产生的文件"必须放在项目文件夹内，并归类到固定位置。**
绝不写入系统临时目录、用户主目录或任何项目外的地方。（见 BUG-005）

| 类别 | 位置 |
| --- | --- |
| 数据库 / WAL / 备份 / 测试库 / 日志 / 导出文件 | `data/` 下的对应子目录 |
| Python 字节码缓存、测试缓存 | 项目内（`__pycache__`、`.pytest_cache`），用 `clean.bat` 清理 |

判断标准：**用户把整个 `to_do_list` 文件夹复制走，程序应能完整工作，且不残留任何外部文件。**

## 3.2 临时文件纪律

- **临时测试脚本用完立刻删除。** 不要留在 `scripts/` 里 —— 它们会造成混淆，
  也可能在以后被误当成正式脚本运行。
- 临时脚本统一用 `_` 开头命名（如 `scripts/_check_xxx.py`），方便一眼识别。
- 脚本产生的中间文件放进 `data/test/`，用完清理。
- **不要碰用户正在运行的东西**（进程、端口、数据库连接）。

## 4. 沟通约定

- 每实现一个功能，按六步交付：设计思路 → 代码 → 逐行解释关键代码 → 如何运行 → 测试方法 → Bug 排查。
- 出现 Bug 时**先带用户分析原因，再修改代码**。
- 面向初学者：解释"为什么这样做"，而不只是"怎么做"。
- **凡是要求用户敲命令的地方，必须交代"在哪个文件夹里敲"**，并尽量提供双击版 `.bat` 入口。
  （见 BUG-004：只写"运行 xxx 命令"会让初学者完全卡住。）
- 文档里不要写"凭印象"的信息（日期、路径、版本号），**一律先执行命令查询**。（见 BUG-003）
