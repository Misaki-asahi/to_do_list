# 项目现状说明（PROJECT.md）

> **这份文档写给谁看**
> 写给「接手这个项目的人」——包括三个月后的你自己。
> 它回答四个问题：**做到哪了？为什么这么做？还差什么？下一步做什么？**
>
> **它和 README.md 的分工**
> - `README.md` —— **给使用者看的**：怎么装、怎么用。不含任何开发内部信息。
> - `PROJECT.md`（本文）—— **给开发者看的**：架构、决策、进度、债务。
>
> **最后更新**：2026-09-15（v0.4.5 已发布：第二轮全面测试 + 安全复查，修掉 7 个缺陷，已推送 GitHub 并打标签）

---

## 一、一句话现状

**v0.4.5：第二轮全面测试 + 安全复查，查出 7 个缺陷（BUG-049 ~ BUG-055）并全部修掉，测试全绿，已发布（标签 v0.4.5）。**

**v0.4.x 这一串都做了什么（一句话各一条）：**

| 版本 | 做了什么 |
| --- | --- |
| v0.4.0 | **一轮完整验收测试**：23 条需求逐条验证 + 7 个薄弱点复核 + 安全威胁模型 → 查出并修掉 13 个缺陷；新增顶部「编辑」按钮 |
| v0.4.1 | 修界面三处（工具栏被压扁 / 「日期▾」被挤成 1 像素 / 长标题被硬裁）+ 弹窗可以鼠标调整大小 |
| v0.4.2 | 按用户要求调出厂设置：字号 **10**、不透明度 **0.90**、悬浮窗与开机自启 **默认开启且静默**；上下留白放宽 |
| v0.4.3 | 修「提醒日期格式错 → 整条待办存不进去」（BUG-048）+ **放宽日期格式**（中英文皆可、"30分钟后"）|
| v0.4.4 | **第二轮全面测试**：真窗口真点击 + 真浏览器（Chrome CDP）+ 真子进程 + 像素级测量 → 查出 6 个缺陷（BUG-049 ~ BUG-054）并全部修掉 |
| v0.4.5 | **发布前安全复查**：跨站防护只比主机名不比端口（BUG-055）→ 收紧到「协议+主机+端口」三件套，并补 7 条测试 |

**出厂设置现在就是"用户自己正在用的那套"**（v0.4.2 起）：

| 项 | 出厂默认 |
| --- | --- |
| 背景 / 文字 | 纯黑 / 白色，宋体 |
| 字号 | **10 磅**（= 用户当前设置） |
| 不透明度 | **0.90**（= 透明度 10%，也是用户当前设置） |
| 悬浮窗 | **默认开启并显示** |
| 开机自启 | **默认开启，而且静默**（不弹浏览器，只把悬浮窗显示出来） |
| 置顶 | 否（"任何窗口都能覆盖它"） |

**最值得记住的两条结论：**

1. **「代码能跑起来不报错」和「功能真的能用」是两件事。**
   日历选择器从 v0.3.2 做出来那天起就**一次都没能用过**（点一下就抛异常、
   留下一个关不掉的空盒子），而 CHANGELOG 和待优化清单当时都写着"已完成"——
   原因是**没人真的点开过它**，测试也只覆盖了"接口面"。
2. **"默认值"的取舍取决于"用户是谁"。**
   悬浮窗最初默认关闭，理由是"新功能默认关闭是基本的礼貌"——
   那默认服务的是"陌生用户"。而这个项目只有一个使用者，
   他要的是"开机就能用"。所以 v0.4.2 把默认改成了开启。

**尚未打包、未打标签、未推送 —— 代码已就绪，等确认后再发。**

| 维度 | 状态 |
| --- | --- |
| 功能完整度 | ✅ 需求 1–9 全部实现；**需求 10：桌面悬浮窗（v0.3.0 新增 + v0.3.1 重做 + v0.4.0 验收修复）** |
| **验收测试（v0.4.0）** | ✅ **已完成一轮完整验收**：23 条需求逐条验证 + 7 个薄弱点独立复核 + 安全威胁模型。查出 12 个缺陷（BUG-033 ~ BUG-044）+ 1 个测试缺陷（BUG-045），**全部已修** |
| 自动化测试 | ✅ **全部通过**（在 22:45 这个曾必红的时段跑的）。v0.4.0 还修掉了 2 个"每天 22:30 之后必红"的断言 |
| 需求理解 | ✅ **v0.3.3 澄清了两处，都是我先理解错的**：<br>① 「透明度 30%」= **不透明度 0.70**（我曾直接写成 0.30，方向反了）<br>② 「字体清晰」指的是**锐利度**，由 DPI 渲染决定，**和透明度无关**（我曾把"清晰度"和"对比度"当成同一件事，还据此做了一个白做的取舍） |
| 异常处理与日志 | ✅ 已完成（**v0.3.1 补上了悬浮窗进程的日志**，见 BUG-030） |
| 数据安全（回收站） | ✅ 已完成（阶段三-03，软删除 + 还原 / 彻底删除 / 清空） |
| 打包发布（0.2.0） | ✅ 安装包 32.6 MB + 免安装版 zip 19.9 MB，均做过端到端验证 |
| **v0.3.3 打包** | ⏳ **尚未执行**。因为不再排除 tkinter，安装包预计比 0.2.0 大约 10 MB，需实测确认 |
| 悬浮窗实机验证 | ✅ 已跑通（隔离环境 port 8123，未碰真实数据）。含"置顶能拍到 / 不置顶被盖住"的对照实验 |
| 代码重构 | 🔨 进行中（见第七节） |
| **GitHub 发布** | ✅ **v0.4.5 已推送**（提交 `2dcb42e`，标签 `v0.4.5`）；v0.2.0 也在（标签 `v0.2.0`）。安装包 zip 由用户上传到 Releases |

### 仓库位置（重要）

| 项 | 值 |
| --- | --- |
| 仓库根目录 | **`win11/`** —— `.git` 在这里，所有 `git` 命令都在 `win11/` 执行 |
| 远程 | `https://github.com/Misaki-asahi/to_do_list.git` |
| 分支 / 标签 | `master` / `v0.2.0`（`v0.3.0`、`v0.3.1` 待打） |
| 不在仓库里的 | 上层 `win10/`、根目录的版本说明文件（`README.md` 等） |

**代码规模**：约 15,500 行（不含文档，悬浮窗功能约 5,200 行）；
文档 16 篇 Markdown，另有同名 Word 版 ——
**`.docx` 是 `.md` 的派生产物，不进 Git**，用 `python scripts/md2docx.py --all` 一键生成。

---

## 二、项目定位与需求对照

**一句话定位**：本地优先（Local-First）的离线待办与提醒工具。断网可用，数据只存在自己电脑上。

| # | 你的原始需求 | 实现方式 | 状态 |
| --- | --- | --- | --- |
| 1 | 完全离线编辑 + 提醒 | SQLite 本地单文件 + 系统级 Windows 通知 | ✅ |
| 2 | 后端 Python + 前端网页 | FastAPI + 原生 HTML/CSS/JS（零构建） | ✅ |
| 3 | 本地持久化 | `data/todo.db`（WAL 模式）+ 自动备份 | ✅ |
| 4 | 增 / 查 / 改 / 删 / 完成筛选 | REST API + 列表页 + 编辑弹窗 | ✅ |
| 5 | 结构清晰可扩展 | 三层架构 routers → services → repositories | ✅ |
| 6 | 提醒弹窗 | 右下角常驻通知卡片 + Windows 原生 Toast | ✅ |
| 7 | 开机自启 + 独立界面 | 启动文件夹快捷方式 + `/settings` 页 | ✅ |
| 8 | 日历导入、未来日期建任务、分钟级提醒 | 月历视图（决策 2A：不做 .ics） | ✅ |
| 9 | 可发布到 GitHub / 公网 | `.gitignore` / LICENSE / README / 安装包 | ✅ v0.2.0 已发布 |
| 10 | **桌面悬浮窗**（v0.3.0） | tkinter 独立进程 + 本机 HTTP；可移动/缩放/调透明度字号颜色字体；窗口内编辑与提醒；按日期/优先级/分类分块；设置里开关；随开机自启出现 | ✅ 实现完成，⏳ 未打包 |

---

## 三、已完成的工作

### 阶段一：项目设计
- 产出 `docs/阶段一-项目设计.md`（+ Word）：架构设计、模块划分 M1–M8、6 个决策点。
- **确定了三层架构和 6 个关键技术决策**（见第四节）。

### 阶段二：功能实现（8 篇文档）

| 文档 | 内容 |
| --- | --- |
| 阶段二-01 环境搭建与第一个网页 | Python 环境、FastAPI 起服务、第一个页面 |
| 阶段二-02 数据库与数据表 | SQLAlchemy ORM、tasks / settings 两张表 |
| 阶段二-03 添加待办 | 第一个完整的前后端往返（表单 → API → 数据库 → 列表） |
| 阶段二-04 完整任务管理 | 编辑弹窗、删除、完成切换、筛选、搜索、统计 |
| 阶段二-05 提醒系统 | 提醒扫描、防重复提醒（reminded_at）、提醒收件箱 |
| 阶段二-06 日历视图 | 月历网格、点未来某天建任务、当天任务明细 |
| 阶段二-07 设置页与开机自启 | 自启开关、偏好设置、日志查看、手动备份 |
| 阶段二-08 系统级提醒重构 | 从「浏览器内弹窗」升级为「Windows 原生 Toast」 |

**阶段二之后追加的两次用户反馈改造**（已并入上面的文档）：
- OPT-018：编辑改为**弹窗**（原来是就地填回顶部表格，看不清）。
- OPT-019：提醒改为**系统级通知**（右下角、不点不走、浏览器关掉也能收到）。

### 阶段三：工程化

| 文档 | 内容 | 状态 |
| --- | --- | --- |
| 阶段三-01 打包与发布 | PyInstaller 打包 exe + 自研安装器（无 Inno Setup 依赖） | ✅ |
| 阶段三-02 异常处理与日志 | 统一异常体系、文件日志、全局异常钩子、请求日志中间件 | ✅ |
| 阶段三-03 单元测试与代码重构 | 229 项 pytest + 模块拆分重构 | 🔨 **文档未写** |

**配套工程产物**：

| 文件 | 作用 |
| --- | --- |
| `app/core/errors.py` | 异常基类体系：AppError / BusinessError / NotFoundError / ValidationError / ForbiddenError |
| `app/core/logging_setup.py` | 文件日志（按天轮转、保留 14 天）+ 全局异常钩子 |
| `app/core/backup.py` | 启动自动备份、保留最近 7 份 |
| `tests/` | 14 个测试文件、**481 项测试**（v0.2.0 时是 11 个文件 259 项） |
| `scripts/check_repo.py` | 数据层自检 34 项 |
| `scripts/check_errors.py` | 异常处理自检 30+ 项 |
| `scripts/smoke_test.py` | 冒烟测试：页面 + 静态资源 + 接口连通性 |
| `scripts/_isolation.py` | **测试隔离工具（数据安全第一道防线）** |
| `build.bat` / `build_installer.bat` | 一键打包 exe / 一键生成安装程序 |
| `.gitignore` / `LICENSE` / `README.md` | 发布准备（README 只面向使用者） |

### 阶段四：桌面悬浮窗（v0.3.0）

| 文档 | 内容 | 状态 |
| --- | --- | --- |
| `docs/阶段四-01-桌面悬浮窗.md` | 悬浮窗的完整设计与实现（六步交付） | ✅ |

**新增的代码结构**：

```
app/gui/                        ★ 悬浮窗界面（只在"用户开了这个功能"时才被启动）
├── __init__.py                 包说明（含"只许走 client.py 取数据"的约定）
├── client.py                   悬浮窗进程唯一的网络出口（相当于桌面版 api.js）
└── window.py                   tkinter 画的窗口本身（≈1290 行）
app/repositories/floating_repo.py   悬浮窗设置的存取（复用 settings 表，表驱动）
app/services/floating_service.py  ★ 业务核心：校验/分块/数据组装/进程管理/守护线程
app/routers/floating.py          接口层（设置页用 + 窗口用两组，后者要令牌）
data/floating/                   运行产物（pid / 锁 / 窗口日志），已随 data/ 排除
```

---

## 四、关键决策记录（重要，别推翻）

### 4.1 你在阶段一确认的 6 个决策

| # | 决策点 | 你的选择 | 影响 |
| --- | --- | --- | --- |
| 1 | 界面形态 | **C** 网页版优先，第三阶段套桌面外壳 | 现阶段是浏览器网页版 |
| 2 | 「导入日历」的含义 | **A** 只要日历视图 | **不做 .ics 导入导出**，省掉一整层兼容性坑 |
| 3 | 时间存储 | **B** 纯本地时间字符串 | 存 `YYYY-MM-DD HH:MM` |
| 4 | 多人使用 | **A** 只预留不实现 | 保留 user_id 且恒为 1，不做登录 |
| 5 | 后端框架 | **A** FastAPI | 白送 /docs 调试页 + 自动参数校验 |
| 6 | 能否联网装依赖 | **A** 可以 | 但运行时依赖仍压到最少：fastapi / uvicorn / sqlalchemy / pydantic |

### 4.2 实现阶段追加的架构决策

| 决策 | 内容 | 为什么 |
| --- | --- | --- |
| **3B 落地细节** | 时间统一用**定宽**本地字符串 `YYYY-MM-DD HH:MM` | 定宽 ⇒ **字符串排序结果 == 时间排序结果**，数据库能直接排序，不用转换 |
| **提醒不用第三方库** | 用标准库 threading 守护线程，每 20 秒扫一次 | 少一个依赖 = 少一个「别人装不上」的风险 |
| **通知不用第三方库** | 直接调 Windows 自带 PowerShell 5.1 发原生 Toast | 不引入 winotify / win10toast |
| **前端零构建** | 原生 JS，不用 npm / webpack | 下载即跑，不需要 Node 环境 |
| **单一网络出口** | 所有请求必须走 `app/static/js/api.js` | 想换接口地址、加统一错误处理，只改一个文件 |

### 4.3 ★ 悬浮窗的六个架构决策（v0.3.0 / v0.3.1，别推翻）

| 决策 | 内容 | 为什么 |
| --- | --- | --- |
| **用 tkinter，不加依赖** | 界面用 Python 标准库画 | 「下载下来就能跑」是本项目第一条原则。为此引入几十 MB 的 GUI 框架不划算。代价是界面朴素、整窗透明（不能只让背景透明） |
| **窗口是独立进程，不是线程** | 服务端 spawn 一个子进程画窗口 | tkinter 的界面**必须在主线程**跑 `mainloop()`，而服务的主线程被 uvicorn 占着。顺带换来「窗口卡住不拖累服务」「服务重启窗口不死」 |
| **窗口走本机 HTTP 读数据** | 不直连 SQLite、不 import repositories | ① 两进程同时写库会撞 `database is locked`；② 数据逻辑只维护一份。代价是一次本地调用（实测 1–3 ms） |
| **窗口令牌** | `/api/floating/*` 中能读写数据的接口要求 `X-Float-Token` | 防误触、防别的网页瞎调。**不是完整鉴权**——服务只监听 127.0.0.1，真要防黑客靠"不对外暴露端口"这件事本身 |
| **两道闸门防重复开窗** | 服务端线程锁 + 窗口进程文件锁（`msvcrt.locking`） | "点两次开启叠出两层窗口"是这类功能最常见的 Bug。文件锁在进程被杀时由系统自动释放，比 pid 文件可靠 |
| **打包时不再排除 tkinter** | `OfflineTodoList.spec` 去掉 `--exclude-module tkinter` | 0.2.0 时代为省体积排除了它；现在悬浮窗**就是**用 tkinter 画的，继续排除会让安装版静默失效。代价是安装包约 +10 MB |

### 4.4 ★ 悬浮窗的定位与优先级（用户反复强调的两条，别推翻）

**① 它是"桌面便签"，不是"浮在最上层的工具窗"。**

用户原话：「悬浮窗要设于桌面，任何窗口都能覆盖」。
所以 `FLOATING_ALWAYS_ON_TOP` 默认是 **False** ——
你打开浏览器工作时它会被挡在后面，按 Win+D 回桌面时它就在那里。

因为"被挡住就点不到"，所以**必须保留三个"叫它回来"的入口**：
设置页的「立即显示」、窗口右键菜单的「总在最前面」、⚙ 设置弹窗里的同名开关。

**② 字体清晰是排版的前提，它优先于"能调得很透"。**

用户原话：「保持字体的高清晰度为排版的最基本前提」。
这条不是"加一个功能"，而是给一堆互相冲突的要求**排了序**：

| 冲突 | 谁让路 | 落地成什么 |
| --- | --- | --- |
| 想要很透 ↔ 想要字清楚 | **透明让路** | 透明度默认 92%、下限 80%（不允许更低） |
| 界面漂亮 ↔ 字不糊 | **字不糊优先** | 声明 DPI 感知，让系统别拉伸窗口 |
| 字号选择自由 ↔ 渲染锐利 | **锐利优先** | 只给整数磅（非整数会被取整，边缘可能错位半个像素） |

**规律：多个要求冲突时，让用户定优先级，然后把优先级写成默认值和取值范围** ——
而不是写在文档里当口号。

### 4.5 ★ 数据安全铁律（用一次真实事故换来的）

> **背景**：BUG-011 —— 一个临时测试脚本误操作了**真实数据库**，把你手动创建的待办**全部删除且无法恢复**。
> 事故详情见 `docs/问题记录与解决方案.md`。

由此确立的五条铁律（写在 `AGENTS.md` 第 3.0 节，任何情况下不得违反）：

1. **任何写数据的脚本，必须先切到隔离数据库**（use_isolated_db + assert_isolated）。
2. **会删数据的函数必须带 confirm=True 参数**，不传就抛异常。
3. **防数据丢失的功能要「最先做」，不是「优先级最高但排最后」。**
4. **永远不要替用户判断「这些数据不重要」。**
5. **改动数据前先备份。**

### 4.6 环境与仓库决策（2026-09-13 追加）

| 决策 | 内容 | 为什么这么做 |
| --- | --- | --- |
| **仓库根目录 = `win11/`** | `.git` 放在 `win11/` 里，不再放上层 `to_do_list/` | 上层现在同时有 `win10/` 和 `win11/` 两个版本；把仓库收进 `win11/` 后**只有它会被推送到 GitHub**，`win10/` 完全留在本地 |
| **`.docx` 不进 Git** | `.gitignore` 排除 `*.docx`，Word 版按需生成 | 和 `build/`、`dist/` 同理：**派生产物不上传**。而且 `.docx` 是二进制，改 3 个字会显示成几万行 diff，Git 历史会没法看 |
| **新增 `md2docx.py --all`** | 一条命令生成全部 Word | `.docx` 不进 Git 之后，别人（和未来的你）必须能一键重建它们 |
| **安装包不进 Git** | `dist/`、`build/` 已排除 | 32.6 MB 二进制；要给别人下载就用 GitHub Releases 挂附件 |
| **标签是版本快照** | `v0.2.0` 创建后，对应的提交不再改动 | 标签指向某个提交，改提交会让标签和它脱节 |

---

## 五、目录结构地图

（以 **`win11/`** 为基准 —— 它既是项目目录，也是 Git 仓库的根目录）

```
app/                          ← 后端主体
├── config.py                 ★ 所有常量、路径、端口、悬浮窗的 30+ 个默认值
├── database.py               引擎 / 会话 / 建表
├── models.py                 数据库表结构（SQLAlchemy）
├── schemas.py                接口收发格式（Pydantic）
├── main.py                   应用装配、中间件、全局异常处理
├── core/                     与业务无关的基础设施
│   ├── timeutil.py           ★ 时间转换的唯一出口
│   ├── errors.py             异常体系（含 ForbiddenError）
│   ├── logging_setup.py      日志
│   ├── backup.py             自动备份
│   ├── scheduler.py          提醒扫描线程
│   ├── notifier.py           Windows 原生通知
│   └── autostart.py          开机自启（支持 --floating）
├── gui/                      ★ 桌面悬浮窗界面（v0.3.0 新增，独立进程）
│   ├── client.py             窗口唯一的网络出口
│   └── window.py             tkinter 窗口
├── repositories/             ★ 唯一碰数据库的一层
├── services/                 业务规则层（floating_service 是悬浮窗核心）
├── routers/                  HTTP 接口层（floating.py 是悬浮窗接口）
├── templates/                4 个页面（index / calendar / trash / settings）
└── static/                   js（8 个）/ css

tests/                        14 个测试文件、499 项自动化测试
scripts/                      自检 / 测试 / 文档生成工具
installer/                    安装程序源码
docs/                         16 篇文档 + 问题记录 + 待优化清单
                              （同名 .docx 由 --all 生成，不进 Git）

data/                         ★ 你的数据（已被 .gitignore 排除）
├── todo.db                   主数据库
├── backups/                  自动备份
├── logs/                     运行日志
├── floating/                 ★ 悬浮窗的 pid / 文件锁 / window.log（v0.3.0）
└── test/                     测试隔离目录

run.py                        程序入口（--no-browser / --floating / --floating-stop）
start.bat / start_silent.bat  启动（双击即可）
check.bat / clean.bat         自检 / 清理
build.bat / build_installer.bat 打包

README.md                     ★ 给使用者
PROJECT.md                    ★ 给开发者（本文）
CHANGELOG.md                  版本变更史
AGENTS.md                     ★ 开发约定（AI 助手也读它）
LICENSE                       MIT
```

**依赖方向（不可逆）**：routers → services → repositories。
数据层**绝不能**反向调用服务层。

---

## 六、质量现状

| 检查手段 | 命令（在 **`win11/`** 里执行） | 结果 |
| --- | --- | --- |
| 单元 / 接口测试 | `python -m pytest tests/` | **499 项全部通过**（退出码 0） |
| 数据层自检 | 双击 `check.bat`，或 `python scripts/check_repo.py` | **通过 35 项**，失败 0 |
| 异常处理自检 | `python scripts/check_errors.py` | **全部通过** |
| 冒烟测试 | 先跑 `python run.py`，再 `python scripts/smoke_test.py` | 页面 / 静态资源 / 接口连通性 |
| tkinter 可用性 | `python -c "import tkinter; print(tkinter.TkVersion)"` | **8.6**（悬浮窗需要它） |
| 仓库完整性 | `git fsck` | 无孤儿对象、无损坏 |
| 工作区干净度 | `git status` | ⏳ v0.3.0 的改动尚未提交 |

**测试隔离**：`tests/conftest.py` 与 `scripts/_isolation.py` 会在导入 app **之前**
把数据库切到 `data/test/`，所以跑测试**不会碰你的真实待办**。
另外 `tests/conftest.py` 里还有一个装置：**禁止测试真的去启动悬浮窗进程**
（否则跑一次测试，屏幕上会多出一堆小窗口）。
它替换的是「真正调用 `subprocess.Popen`」那一步，所以
「启动参数对不对」照样会被测到 —— 这一点很关键，详见 BUG-027。

**已知测试空白**：

- `/api/settings/shutdown` 只验证了它在接口文档里存在，没有真的调用（调用它会杀掉测试进程本身）。
- 悬浮窗的**界面外观**（窗口长什么样、拖动跟不跟手）没有自动化测试 ——
  tkinter 没有像样的自动化手段，只能靠人眼看着确认。
  但界面里的**纯逻辑**（颜色换算、对比色、配置变更检测）都有测试。

---

## 七、未解决的问题

### 7.1 已确认事项（无遗留）

> 下面三项曾经是发布前的阻塞点，现均已关闭。
> 记录在此，是为了说明**当时卡在哪、最后怎么定的** ——
> 这类信息在项目交接时最有用，丢了就再也想不起来。

| # | 事项 | 结论 |
| --- | --- | --- |
| Q1 | LICENSE 的版权人 | 已确定为 `Copyright (c) 2026 Misaki`（占位符尖括号已去掉） |
| Q2 | 安装包能否真的安装 / 运行 / 卸载 | 已完成端到端验证：静默安装 → 运行安装版访问 `/trash` ✅ → 静默卸载 → 系统清理干净。数据目录 `%LOCALAPPDATA%\OfflineTodoList` 按设计保留。详见 `docs/阶段三-01-打包与发布.md` 附录 |
| Q3 | 是否做回收站 | 已在 v0.2.0 实现，见 `docs/阶段三-03-回收站（软删除）.md` |
| Q4 | 项目里要不要抹掉「AI 助手参与开发」的痕迹 | **决定保留**。共有 4 处提及（`AGENTS.md`、`PROJECT.md`、`docs/待优化与已知问题.md`、`docs/阶段一-项目设计.md`）。理由：这本来就是事实，且「给协作者 / AI 助手看的约定」在当下的开源项目里很常见。**此项已决策，无需再议。** |

### 7.2 技术上的已知问题

> **★ v0.4.0 更新**：下面这一节是"验收测试之前"的状态。
> 验收查出的 12 个缺陷（BUG-033 ~ BUG-044）**已全部修复**，
> 详见 `docs/问题记录与解决方案.md` 的索引表和文末「v0.4.0 修复记录」。
> 其中影响最大的是三条：日历选择器完全不可用（BUG-033）、
> 守护线程的失败退避永不触发（BUG-034）、并发开窗喷进程 + 500（BUG-035）。

| # | 问题 | 严重度 | 说明 |
| --- | --- | --- | --- |
| 1 | ~~删除是硬删除，没有回收站~~ **已修复** | — | OPT-001 / OPT-011。已改为软删除：删除 = 移到回收站，可还原。**这是本项目风险最高的一处缺口，现已堵上。** |
| 2 | ~~BUG-019 重定向输出时崩溃~~ **已修复** | — | ✅ 字符在重定向时（GBK 编码）触发 UnicodeEncodeError。已改为纯文本 + 新增 `scripts/_console.py` 输出保护。 |
| 2b | ~~BUG-020 硬编码本机绝对路径~~ **已修复** | — | `check_errors.py` 写死了开发者本机的绝对路径，换台电脑就失效，还泄露了本机目录结构。已改为从 `__file__` 推算。 |
| 2c | ~~BUG-021 自检脚本重复运行误报失败~~ **已修复** | — | 日志跨次运行累积，导致「只记录一次」类断言第 2 次必失败。已在导入 app 前清空隔离日志。 |
| 3 | ~~BUG-018 尚未归档~~ **已归档** | — | strptime 对 `2026-9-13 8:30` 这种**没补零**的输入也会解析成功，导致非规范字符串入库、字符串排序失效。已用「往返校验」修复，并补写了单元测试。 |
| 4 | 代码重构未收尾 | 低 | `scripts/md2docx.py` 的 convert（110 行）、`scripts/clean.py` 的 main（65 行）。check_repo.py 已完成。 |
| 5 | 缺少前端自动化交叉检查 | 中 | OPT-017。之前靠**临时脚本**抓出过 BUG-008、BUG-015 两个真实 Bug，但脚本没留下来。 |
| 6 | 文档与现实脱节 | 中 | `docs/阶段三-03-单元测试与代码重构.md` 未写；`docs/待优化与已知问题.md` 里 OPT-012（已写 499 项测试）、OPT-015（已能打包）的状态已过时。BUG-018 ~ 022、026、027 均已归档完毕。 |
| 7 | OPT-016：后台标签页定时器被浏览器降频 | 中 | 提醒最多延迟约 1 分钟。**实际影响已大幅降低**——主通知现在是后端线程发的系统通知，不依赖浏览器定时器了。 |
| 8 | ~~OPT-024 从设置页改窗口坐标不会立刻生效~~ **已修好** | — | v0.3.1 重做界面时把"应用几何"统一到一个函数、位置和尺寸各管各的，实测已生效（设置了 (640,150)，窗口就在那里）。现在改位置的延迟上限是轮询间隔 3 秒（设计如此）；窗口自己拖动是立即的 |
| 9 | ~~OPT-025 守护线程缺少"失败退避"~~ **已完成** | — | v0.3.2 补上：连续失败 3 次放慢到 30 秒、10 次停下并报原因；设置页新增「自动重试状态」一行（正常时隐藏）；用户手动重试即解除 |
| 10 | **v0.3.3 尚未打包 / 打标签 / 推送** | — | 代码已就绪。打包时注意：安装包会比 0.2.0 大约 10 MB（因为不再排除 tkinter），需实测确认 |
| 11 | ~~OPT-027 / 029~~ **已完成** | — | v0.3.2 补上：编辑弹窗的分类栏永远有「＋ 新分类」按钮；截止/提醒时间旁边有 📅 日历选择器（可翻月、点日期、带今天/明天/下周一/清空快捷键）。剩余 OPT-030（逐控件透明）与 OPT-026 同源，换 GUI 框架才能根治 |
| 12 | ~~多进程初始化约定写进 AGENTS.md~~ **已完成** | — | v0.3.2 已写入 AGENTS.md 3.2「多进程约定」（日志初始化 + 无控制台时报错 + 不直连数据库），并顺带补了 3.5「改文档的转义纪律」（BUG-031 的教训） |

完整的 30 条待优化项见 `docs/待优化与已知问题.md`。

---

## 八、下一步任务（按建议顺序）

### ✅ 第 0 优先：全面测试 + Bug 修复 —— **已完成（v0.4.4）**

**结果**：16 条薄弱点逐条独立复核 + 自找死角，查出 **6 个真实缺陷**
（BUG-049 日期差 12 小时 / BUG-050 关窗卡 3 秒且被强杀 / BUG-051 假 CRITICAL 日志 /
BUG-052 长标题被硬裁 / BUG-053 提醒没有日志 / BUG-054 文档与实现不符），
**全部已修**，并补了防复发测试。**结论表见 `docs/交接说明-给下一个会话.md` 第六节末尾。**

<details><summary>（下面保留当时的任务描述）</summary>

### 第 0 优先：全面测试 + Bug 修复（用户 2026-09-15 指定）

**用户原话**：「我的下一步是要全面测试和bug修复」。

**重点看哪里**：`docs/交接说明-给下一个会话.md` **第六节「已知薄弱点」**——
那里列了 16 条"我怀疑、但没验到"的地方，分成四类：

| 类别 | 条数 | 最容易出问题的是 |
| --- | --- | --- |
| 界面交互 | 1~7 | **✎ 编辑按钮、设置弹窗的取色器、设置页的 6 个配色色块** —— 这三处**从来没被真人点过** |
| 时序与并发 | 8~10 | v0.4.0 新加的"拉起冷却期"会不会挡住正当的「重启窗口」 |
| 打包与发布 | 11~12 | **v0.3.x / v0.4.x 从来没打过包** |
| 数据与时间 | 13~16 | 日期解析的边角、数据量大时的表现、提醒真的到点 |

**方法建议**（v0.4.0 那一轮验证有效的四种手段，见下文）：

| 手段 | 能查出什么 | 例子 |
| --- | --- | --- |
| 把窗口真的画出来再数控件 | 布局类缺陷 | BUG-036 / BUG-037 |
| 真的起子进程（不用 mock） | 进程/时序缺陷 | BUG-034 / BUG-035 |
| 像素级代数验证 | 渲染类"感觉"问题 | "降低不透明度会不会让字变糊" |
| 打完并发再跑一遍 | 竞态 | BUG-035 |
| **在"不友好的时刻"再跑一遍** | 时段性假红 | BUG-045（每天 22:30 必红） |

**纪律**：见 `docs/交接说明-给下一个会话.md` 第七节（特别是第 3 条：
**不要在测试里碰项目文件夹以外的东西** —— BUG-047 的教训）。

### ⏳ 第 0 优先（等用户说"可以删了"）：删掉一条误建的开机自启项

| 项 | 内容 |
| --- | --- |
| **要删什么** | `%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\OfflineTodoList.vbs` |
| **它是什么** | 一条**开机自启项**（静默：`start_silent.bat --no-browser --floating`，会在登录时把服务跑起来并显示悬浮窗） |
| **为什么会有** | **开发过程中误建的** —— v0.4.2 加"默认开启开机自启"时，测试用的 TestClient 触发了启动钩子，真的写进了系统启动文件夹。详见 `docs/问题记录与解决方案.md` 的 **BUG-047** |
| **用户怎么说的** | 「**等我测试完成发布后，你再帮我把vbs删掉**」（2026-09-15） |
| **什么时候删** | **用户测试完成 + 发布（打包/打标签/推送）之后**。在那之前**保持原样** —— 用户测试期间正需要它（开机自动起来） |
| **怎么删** | 直接删掉那个文件；或者在设置页把「开机自启」开关关掉（关掉后 `autostart_user_disabled` 会被记成 "1"，程序不会下次启动又自动补建） |
| **删完确认** | 看启动文件夹里没有 `OfflineTodoList.vbs`；设置页「开机自启」应显示为关闭 |

> ⚠️ **不要提前删**：这是用户明确要求的顺序（先测、先发，最后再清理）。

### ✅ 第 0 优先：验收测试 —— 已完成（v0.4.0）

1. ✅ 按需求清单逐条验证（悬浮窗 14 条 + 最初 9 条）。
2. ✅ Bug 检查：上一轮怀疑的 7 个薄弱点**逐条独立复核**（其中 4 条坐实为真缺陷）。
3. ✅ 安全性检查：写清了威胁模型（防住什么 / 没防住什么），并修掉了跨站写这一条。
4. ✅ 报告 + 按用户确认逐条修复。

**这一轮留下来的"测试方法"值得复用**（见 `docs/问题记录与解决方案.md` 文末）：

| 手段 | 能查出什么 | 例子 |
| --- | --- | --- |
| 把窗口真的画出来再数控件 | 布局类缺陷 | BUG-036 / BUG-037 |
| 真的起子进程（不用 mock） | 进程/时序缺陷 | BUG-034 / BUG-035 |
| 像素级代数验证 | 渲染类"感觉"问题 | "降低不透明度会不会让字变糊" |
| 打完并发再跑一遍 | 竞态 | BUG-035 |
| 在"不友好的时刻"再跑一遍 | 时段性假红 | BUG-045 |

</details>

### 第 1 优先：提交与打包

5. **把 v0.3.3 的改动提交并推送**（用户确认后）：
   `git add -A` → `git commit -m "..."` → `git push`（注意代理要开着，见下方说明）。
6. **打包验证**：双击 `build.bat` → `build_installer.bat`。
   重点确认两件事：① 安装版里悬浮窗能不能开出来；
   ② 安装包体积涨了多少（预期 +10 MB 左右，因为不再排除 tkinter）。
7. **打标签**：`git tag -a v0.3.3 -m "..."`（打完之后对应提交不要再改）。

### ✅ 已完成（v0.3.1 / v0.3.2 收掉的历史遗留）

| 原来排在"未解决"里的 | 现在的状态 |
| --- | --- |
| OPT-024 从设置页改窗口坐标不生效 | ✅ v0.3.1 统一"应用几何"后已生效 |
| OPT-025 守护线程缺"失败退避" | ✅ v0.3.2 完成（3 次放慢 / 10 次停下 / 手动重试解除） |
| OPT-027 不能新建分类 | ✅ v0.3.2 完成（编辑弹窗永远有「＋ 新分类」） |
| OPT-029 时间只能手打 | ✅ v0.3.2 完成（📅 日历选择器） |
| 多进程初始化约定写进 `AGENTS.md` | ✅ v0.3.2 完成（新增 3.2 节） |
| README 补悬浮窗章节 | ✅ v0.3.2 完成 |

### 第 1 优先：剩下的小尾巴

4. **OPT-028：多显示器坐标记忆**（目前只记住一组坐标；
   拔掉副屏时靠"把位置夹回屏幕内"自救）。
5. **OPT-031：日历面板的打磨**（自绘控件，没有年份快速跳转和键盘操作）。
6. **OPT-030：逐控件透明**（与 OPT-026 同源）。
   建议**保持不做** —— 它和"不引入不必要依赖"原则冲突，
   而"下限 80% + 默认 92%"已经把后果控制住了。

### 第 2 优先：把文档和现实对齐（低成本、高收益）

7. 更新 `docs/待优化与已知问题.md`：OPT-012（已写 499 项测试）、
   OPT-015（已能打包）标记为已完成。
8. 写 `docs/阶段三-03-单元测试与代码重构.md`（+ Word）。

### 第 3 优先：收尾工程

9. 把 DOM 交叉检查固化成 `scripts/check_frontend.py`（OPT-017）。
10. 完成剩余重构：`md2docx.py`、`clean.py`。
11. 前端网络断开时的友好提示（OPT-013）。

### 明确「将来再说」的

- 批量操作（OPT-002）、分页（OPT-003）、自定义排序（OPT-004）、深色模式（OPT-006）、
  免打扰时段（OPT-021）、通知点击跳转（OPT-020）、前端模块化（OPT-009）。
- 悬浮窗方面的：
  - **逐控件透明**（只让背景半透明、文字保持清晰）——
    这是 tkinter 的能力边界（它的 `-alpha` 作用在整个窗口上），
    要做得换 GUI 框架，与"零不必要依赖"原则冲突，**暂不做**；
  - 悬浮窗里直接**新建分类**（现在只能在编辑框里手打分类名）；
  - 悬浮窗的**多显示器记忆**（现在只记住一组坐标；
    拔掉副屏时靠"把位置夹回可见范围"来自救）。

### 环境方面的注意事项

#### ★ Git 推送必须先配代理（已定位根因）

**现象**：`git push` 报 `Failed to connect to github.com:443`，但 VS Code 里点推送却是成功的。

**根因**：这台机器上开着代理（`verge-mihomo`，`127.0.0.1:7897`）。
- **VS Code 走 Windows 系统代理**，所以它能推成功；
- **Git 默认【不读】系统代理**，一直在直连 `github.com:443`，因此总被重置。

**解决方式 —— 已配置完成（2026-09-14）**

代理已经写进**本仓库的本地配置**（`.git/config`），全局配置未改动，
所以其它仓库不受影响。**现在直接 `git push` 就能用，不需要带任何参数。**

```powershell
# 已完成，无需重复执行：
git config --local http.proxy http://127.0.0.1:7897
git config --local https.proxy http://127.0.0.1:7897
```

如果哪天需要临时绕过（比如代理关了、用直连），可以单次覆盖：

```powershell
git -c http.proxy= -c https.proxy= push        # 本次直连
```

> ⚠️ **配了代理之后，代理软件必须开着**。哪天关掉 Clash，git 会连不上（连国内仓库也一样）。
> 想取消：`git config --local --unset http.proxy`（`https.proxy` 同理）。
>
> **端口不一定是 7897** —— 以代理软件里显示的为准，可在「设置 → 网络和 Internet → 代理」查看。

---

## 九、常用命令速查

> **⚠️ 所有命令都要在 `win11` 这个文件夹里执行**（见 BUG-004 的教训）。
>
> 也就是你放 `to_do_list` 的那个目录下面的 `win11` —— 它既是项目目录，也是 Git 仓库根目录。

| 我想…… | 怎么做 |
| --- | --- |
| 启动程序 | 双击 `start.bat` |
| 安静启动（不弹浏览器） | 双击 `start_silent.bat` |
| 停掉程序 | 设置页点「退出程序」，或关掉那个黑窗口 |
| **打开/关闭桌面悬浮窗** | 设置页 → 「桌面悬浮窗」→ 打开总开关 |
| **只启动悬浮窗（不起服务）** | `python run.py --floating`（要服务已经在跑） |
| **让悬浮窗退出** | `python run.py --floating-stop` |
| **看悬浮窗的报错日志** | `type data\floating\window.log` |
| **确认电脑有没有 tkinter** | `python -c "import tkinter; print(tkinter.TkVersion)"` |
| 跑全部测试 | `python -m pytest tests/` |
| 只跑悬浮窗的测试 | `python -m pytest tests/test_floating_service.py tests/test_api_floating.py tests/test_gui_utils.py tests/test_autostart_floating.py` |
| 自检（安全，不动真实数据） | 双击 `check.bat` |
| 清理缓存（不动数据库） | 双击 `clean.bat` |
| 打包成 exe | 双击 `build.bat` |
| 生成安装程序 | 双击 `build_installer.bat` |
| 测试提醒能不能弹 | 双击 `test_reminder.bat` |
| Markdown 转 Word（单个） | `python scripts/md2docx.py docs/某文档.md docs/某文档.docx` |
| **重新生成全部 Word** | `python scripts/md2docx.py --all` |

### Git 相关（同样在 `win11/` 里执行）

| 我想…… | 怎么做 |
| --- | --- |
| 看有没有改动要提交 | `git status` |
| 看提交历史 | `git log --oneline` |
| 提交 | `git add -A` 然后 `git commit -m "说明"` |
| 推送到 GitHub | `git push`（连不上时见下面「代理」一行）|
| 推送（走代理） | `git -c http.proxy=http://127.0.0.1:7897 -c https.proxy=http://127.0.0.1:7897 push` |
| 拉取最新 | `git pull` |
| 看标签 | `git tag -n1` |
| 检查仓库完整性 | `git fsck` |
| 清理无用对象（省空间） | `git gc --prune=now` |

> **推送失败的常见原因**：`github.com:443` 连不上（DNS 正常但 TCP 被重置）。
> 这是国内访问 GitHub 的常见网络波动 —— 开代理，或过一会儿重试。**本地开发不受影响。**

---

## 十、文档索引

| 文档 | 内容 |
| --- | --- |
| `README.md` | 给使用者的安装与使用说明（**不含开发内部信息**） |
| `AGENTS.md` | 开发约定：文档产出、Bug 格式、代码规范、**数据安全铁律** |
| `CHANGELOG.md` | 版本变更史 |
| `docs/阶段一-项目设计.md` | 架构设计与 6 个决策点 |
| `docs/阶段二-01 ~ 08` | 每个功能的完整实现过程 |
| `docs/阶段三-01` | 打包与发布 |
| `docs/阶段三-02` | 异常处理与日志 |
| `docs/阶段三-03-回收站（软删除）.md` | 回收站功能的完整设计与实现（六个交付步骤） |
| `docs/阶段四-01-桌面悬浮窗.md` | **悬浮窗**的完整设计与实现（六步交付 + 附录） |
| **`docs/交接说明-给下一个会话.md`** | ★ **新会话 / 新助手的第一份读物**：30 秒上手顺序、需求验收清单（逐条可测）、关键决策、未完成任务、**自查出的测试盲区与可疑点**、工作纪律、故障速查 |
| `docs/问题记录与解决方案.md` | **BUG-001 ~ 032**（023 断号），每条含症状 / 复现 / 报错原文 / 根因 / 修复 / 验证 / 经验 |
| `docs/待优化与已知问题.md` | **OPT-001 ~ 031** 待优化清单（第五、六节是悬浮窗相关项） |
| `docs/上线前检查清单.md` | 发布前要逐条确认的事项 |

---

## 十一、给接手者（或未来的你）的几句话

1. **先读 `AGENTS.md` 的第 3.0 节，再动任何代码。** 那五条铁律是花钱买的。
2. **改数据前先备份。** `data/backups/` 里已经有自动备份，但手动备一份不亏。
3. **遇到 Bug 先记录，再修。** 修完顺便写下「下次怎么避免」——这才是文档的价值。
4. **不要为了「显得专业」引入新依赖。** 这个项目最大的优点之一就是「下载下来就能跑」。
5. **删代码比加代码更需要理由。** 每一处看似绕的写法，背后多半有个真实踩过的坑。
